// Code generated by nuzur go-code-gen. DO NOT EDIT.

package enums

import (
	"encoding/json"
	"github.com/guregu/null/v6"
	"log"
)

//go:generate go run github.com/dmarkham/enumer -type=BoardLinkSource -json
type BoardLinkSource int64

const (
	BOARD_LINK_SOURCE_INVALID = iota
	BOARD_LINK_SOURCE_INSTALLER
	BOARD_LINK_SOURCE_CLI
	BOARD_LINK_SOURCE_AGENT
)

func (e BoardLinkSource) ToInt64() int64 {
	return int64(e)
}

func (e BoardLinkSource) ToNullInt() null.Int {
	return null.NewInt(int64(e), true)
}

func BoardLinkSourceFromString(in string) BoardLinkSource {
	switch in {
	case "invalid":
		return BOARD_LINK_SOURCE_INVALID
	case "installer":
		return BOARD_LINK_SOURCE_INSTALLER
	case "cli":
		return BOARD_LINK_SOURCE_CLI
	case "agent":
		return BOARD_LINK_SOURCE_AGENT
	}
	return BOARD_LINK_SOURCE_INVALID
}

func BoardLinkSourceFromPointerString(in *string) BoardLinkSource {
	if in == nil {
		return BOARD_LINK_SOURCE_INVALID
	}
	return BoardLinkSourceFromString(*in)
}

func (e BoardLinkSource) String() string {
	switch e {
	case BOARD_LINK_SOURCE_INVALID:
		return "invalid"
	case BOARD_LINK_SOURCE_INSTALLER:
		return "installer"
	case BOARD_LINK_SOURCE_CLI:
		return "cli"
	case BOARD_LINK_SOURCE_AGENT:
		return "agent"
	}

	return "invalid"
}

func (e BoardLinkSource) StringPtr() *string {
	val := e.String()
	return &val
}

func BoardLinkSourceSliceToJSON(in []BoardLinkSource) json.RawMessage {
	res := make([]int64, len(in))
	for i, e := range in {
		res[i] = int64(e)
	}
	jr, err := json.Marshal(res)
	if err != nil {
		log.Printf("error marshaling BoardLinkSource slice to json: %v", err)
		// the empty ARRAY, not a zero-length RawMessage: the latter reaches the
		// driver as an empty string, which no JSON column accepts
		return json.RawMessage("[]")
	}
	return jr
}

func JSONToBoardLinkSourceSlice(in []byte) []BoardLinkSource {
	res := []int64{}
	// an absent column (NULL / empty / JSON null) is not a decode failure
	if len(in) == 0 || string(in) == "null" {
		return nil
	}
	err := json.Unmarshal(in, &res)
	if err != nil {
		log.Printf("error unmarshaling BoardLinkSource slice to int slice: %v", err)
		return nil
	}
	if len(res) == 0 {
		return nil
	}
	finalRes := []BoardLinkSource{}
	for _, r := range res {
		finalRes = append(finalRes, BoardLinkSource(r))
	}
	return finalRes
}
