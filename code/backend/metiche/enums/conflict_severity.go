// Code generated by nuzur go-code-gen. DO NOT EDIT.

package enums

import (
	"encoding/json"
	"github.com/guregu/null/v6"
	"log"
)

//go:generate go run github.com/dmarkham/enumer -type=ConflictSeverity -json
type ConflictSeverity int64

const (
	CONFLICT_SEVERITY_INVALID = iota
	CONFLICT_SEVERITY_LOW
	CONFLICT_SEVERITY_MEDIUM
	CONFLICT_SEVERITY_HIGH
	CONFLICT_SEVERITY_CRITICAL
)

func (e ConflictSeverity) ToInt64() int64 {
	return int64(e)
}

func (e ConflictSeverity) ToNullInt() null.Int {
	return null.NewInt(int64(e), true)
}

func ConflictSeverityFromString(in string) ConflictSeverity {
	switch in {
	case "invalid":
		return CONFLICT_SEVERITY_INVALID
	case "low":
		return CONFLICT_SEVERITY_LOW
	case "medium":
		return CONFLICT_SEVERITY_MEDIUM
	case "high":
		return CONFLICT_SEVERITY_HIGH
	case "critical":
		return CONFLICT_SEVERITY_CRITICAL
	}
	return CONFLICT_SEVERITY_INVALID
}

func ConflictSeverityFromPointerString(in *string) ConflictSeverity {
	if in == nil {
		return CONFLICT_SEVERITY_INVALID
	}
	return ConflictSeverityFromString(*in)
}

func (e ConflictSeverity) String() string {
	switch e {
	case CONFLICT_SEVERITY_INVALID:
		return "invalid"
	case CONFLICT_SEVERITY_LOW:
		return "low"
	case CONFLICT_SEVERITY_MEDIUM:
		return "medium"
	case CONFLICT_SEVERITY_HIGH:
		return "high"
	case CONFLICT_SEVERITY_CRITICAL:
		return "critical"
	}

	return "invalid"
}

func (e ConflictSeverity) StringPtr() *string {
	val := e.String()
	return &val
}

func ConflictSeveritySliceToJSON(in []ConflictSeverity) json.RawMessage {
	res := make([]int64, len(in))
	for i, e := range in {
		res[i] = int64(e)
	}
	jr, err := json.Marshal(res)
	if err != nil {
		log.Printf("error marshaling ConflictSeverity slice to json: %v", err)
		// the empty ARRAY, not a zero-length RawMessage: the latter reaches the
		// driver as an empty string, which no JSON column accepts
		return json.RawMessage("[]")
	}
	return jr
}

func JSONToConflictSeveritySlice(in []byte) []ConflictSeverity {
	res := []int64{}
	// an absent column (NULL / empty / JSON null) is not a decode failure
	if len(in) == 0 || string(in) == "null" {
		return nil
	}
	err := json.Unmarshal(in, &res)
	if err != nil {
		log.Printf("error unmarshaling ConflictSeverity slice to int slice: %v", err)
		return nil
	}
	if len(res) == 0 {
		return nil
	}
	finalRes := []ConflictSeverity{}
	for _, r := range res {
		finalRes = append(finalRes, ConflictSeverity(r))
	}
	return finalRes
}
