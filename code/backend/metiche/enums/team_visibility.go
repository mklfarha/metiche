// Code generated by nuzur go-code-gen. DO NOT EDIT.

package enums

import (
	"encoding/json"
	"github.com/guregu/null/v6"
	"log"
)

//go:generate go run github.com/dmarkham/enumer -type=TeamVisibility -json
type TeamVisibility int64

const (
	TEAM_VISIBILITY_INVALID = iota
	TEAM_VISIBILITY_PRIVATE
	TEAM_VISIBILITY_PUBLIC
)

func (e TeamVisibility) ToInt64() int64 {
	return int64(e)
}

func (e TeamVisibility) ToNullInt() null.Int {
	return null.NewInt(int64(e), true)
}

func TeamVisibilityFromString(in string) TeamVisibility {
	switch in {
	case "invalid":
		return TEAM_VISIBILITY_INVALID
	case "private":
		return TEAM_VISIBILITY_PRIVATE
	case "public":
		return TEAM_VISIBILITY_PUBLIC
	}
	return TEAM_VISIBILITY_INVALID
}

func TeamVisibilityFromPointerString(in *string) TeamVisibility {
	if in == nil {
		return TEAM_VISIBILITY_INVALID
	}
	return TeamVisibilityFromString(*in)
}

func (e TeamVisibility) String() string {
	switch e {
	case TEAM_VISIBILITY_INVALID:
		return "invalid"
	case TEAM_VISIBILITY_PRIVATE:
		return "private"
	case TEAM_VISIBILITY_PUBLIC:
		return "public"
	}

	return "invalid"
}

func (e TeamVisibility) StringPtr() *string {
	val := e.String()
	return &val
}

func TeamVisibilitySliceToJSON(in []TeamVisibility) json.RawMessage {
	res := make([]int64, len(in))
	for i, e := range in {
		res[i] = int64(e)
	}
	jr, err := json.Marshal(res)
	if err != nil {
		log.Printf("error marshaling TeamVisibility slice to json: %v", err)
		// the empty ARRAY, not a zero-length RawMessage: the latter reaches the
		// driver as an empty string, which no JSON column accepts
		return json.RawMessage("[]")
	}
	return jr
}

func JSONToTeamVisibilitySlice(in []byte) []TeamVisibility {
	res := []int64{}
	// an absent column (NULL / empty / JSON null) is not a decode failure
	if len(in) == 0 || string(in) == "null" {
		return nil
	}
	err := json.Unmarshal(in, &res)
	if err != nil {
		log.Printf("error unmarshaling TeamVisibility slice to int slice: %v", err)
		return nil
	}
	if len(res) == 0 {
		return nil
	}
	finalRes := []TeamVisibility{}
	for _, r := range res {
		finalRes = append(finalRes, TeamVisibility(r))
	}
	return finalRes
}
