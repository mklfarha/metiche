// Code generated by nuzur go-code-gen. DO NOT EDIT.

package enums

import (
	"encoding/json"
	"github.com/guregu/null/v6"
	"log"
)

//go:generate go run github.com/dmarkham/enumer -type=PlanSource -json
type PlanSource int64

const (
	PLAN_SOURCE_INVALID = iota
	PLAN_SOURCE_INSTANCE_DEFAULT
	PLAN_SOURCE_GRANTED
	PLAN_SOURCE_SUBSCRIPTION
)

func (e PlanSource) ToInt64() int64 {
	return int64(e)
}

func (e PlanSource) ToNullInt() null.Int {
	return null.NewInt(int64(e), true)
}

func PlanSourceFromString(in string) PlanSource {
	switch in {
	case "invalid":
		return PLAN_SOURCE_INVALID
	case "instance_default":
		return PLAN_SOURCE_INSTANCE_DEFAULT
	case "granted":
		return PLAN_SOURCE_GRANTED
	case "subscription":
		return PLAN_SOURCE_SUBSCRIPTION
	}
	return PLAN_SOURCE_INVALID
}

func PlanSourceFromPointerString(in *string) PlanSource {
	if in == nil {
		return PLAN_SOURCE_INVALID
	}
	return PlanSourceFromString(*in)
}

func (e PlanSource) String() string {
	switch e {
	case PLAN_SOURCE_INVALID:
		return "invalid"
	case PLAN_SOURCE_INSTANCE_DEFAULT:
		return "instance_default"
	case PLAN_SOURCE_GRANTED:
		return "granted"
	case PLAN_SOURCE_SUBSCRIPTION:
		return "subscription"
	}

	return "invalid"
}

func (e PlanSource) StringPtr() *string {
	val := e.String()
	return &val
}

func PlanSourceSliceToJSON(in []PlanSource) json.RawMessage {
	res := make([]int64, len(in))
	for i, e := range in {
		res[i] = int64(e)
	}
	jr, err := json.Marshal(res)
	if err != nil {
		log.Printf("error marshaling PlanSource slice to json: %v", err)
		// the empty ARRAY, not a zero-length RawMessage: the latter reaches the
		// driver as an empty string, which no JSON column accepts
		return json.RawMessage("[]")
	}
	return jr
}

func JSONToPlanSourceSlice(in []byte) []PlanSource {
	res := []int64{}
	// an absent column (NULL / empty / JSON null) is not a decode failure
	if len(in) == 0 || string(in) == "null" {
		return nil
	}
	err := json.Unmarshal(in, &res)
	if err != nil {
		log.Printf("error unmarshaling PlanSource slice to int slice: %v", err)
		return nil
	}
	if len(res) == 0 {
		return nil
	}
	finalRes := []PlanSource{}
	for _, r := range res {
		finalRes = append(finalRes, PlanSource(r))
	}
	return finalRes
}
