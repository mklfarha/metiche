// Code generated by nuzur go-code-gen. DO NOT EDIT.

package enums

import (
	"encoding/json"
	"github.com/guregu/null/v6"
	"log"
)

//go:generate go run github.com/dmarkham/enumer -type=ConflictResolution -json
type ConflictResolution int64

const (
	CONFLICT_RESOLUTION_INVALID = iota
	CONFLICT_RESOLUTION_YIELDED
	CONFLICT_RESOLUTION_SPLIT
	CONFLICT_RESOLUTION_MERGED
	CONFLICT_RESOLUTION_CONVERGED
	CONFLICT_RESOLUTION_COORDINATED
	CONFLICT_RESOLUTION_SUPERSEDED
	CONFLICT_RESOLUTION_NOT_A_CONFLICT
)

func (e ConflictResolution) ToInt64() int64 {
	return int64(e)
}

func (e ConflictResolution) ToNullInt() null.Int {
	return null.NewInt(int64(e), true)
}

func ConflictResolutionFromString(in string) ConflictResolution {
	switch in {
	case "invalid":
		return CONFLICT_RESOLUTION_INVALID
	case "yielded":
		return CONFLICT_RESOLUTION_YIELDED
	case "split":
		return CONFLICT_RESOLUTION_SPLIT
	case "merged":
		return CONFLICT_RESOLUTION_MERGED
	case "converged":
		return CONFLICT_RESOLUTION_CONVERGED
	case "coordinated":
		return CONFLICT_RESOLUTION_COORDINATED
	case "superseded":
		return CONFLICT_RESOLUTION_SUPERSEDED
	case "not_a_conflict":
		return CONFLICT_RESOLUTION_NOT_A_CONFLICT
	}
	return CONFLICT_RESOLUTION_INVALID
}

func ConflictResolutionFromPointerString(in *string) ConflictResolution {
	if in == nil {
		return CONFLICT_RESOLUTION_INVALID
	}
	return ConflictResolutionFromString(*in)
}

func (e ConflictResolution) String() string {
	switch e {
	case CONFLICT_RESOLUTION_INVALID:
		return "invalid"
	case CONFLICT_RESOLUTION_YIELDED:
		return "yielded"
	case CONFLICT_RESOLUTION_SPLIT:
		return "split"
	case CONFLICT_RESOLUTION_MERGED:
		return "merged"
	case CONFLICT_RESOLUTION_CONVERGED:
		return "converged"
	case CONFLICT_RESOLUTION_COORDINATED:
		return "coordinated"
	case CONFLICT_RESOLUTION_SUPERSEDED:
		return "superseded"
	case CONFLICT_RESOLUTION_NOT_A_CONFLICT:
		return "not_a_conflict"
	}

	return "invalid"
}

func (e ConflictResolution) StringPtr() *string {
	val := e.String()
	return &val
}

func ConflictResolutionSliceToJSON(in []ConflictResolution) json.RawMessage {
	res := make([]int64, len(in))
	for i, e := range in {
		res[i] = int64(e)
	}
	jr, err := json.Marshal(res)
	if err != nil {
		log.Printf("error marshaling ConflictResolution slice to json: %v", err)
		// the empty ARRAY, not a zero-length RawMessage: the latter reaches the
		// driver as an empty string, which no JSON column accepts
		return json.RawMessage("[]")
	}
	return jr
}

func JSONToConflictResolutionSlice(in []byte) []ConflictResolution {
	res := []int64{}
	// an absent column (NULL / empty / JSON null) is not a decode failure
	if len(in) == 0 || string(in) == "null" {
		return nil
	}
	err := json.Unmarshal(in, &res)
	if err != nil {
		log.Printf("error unmarshaling ConflictResolution slice to int slice: %v", err)
		return nil
	}
	if len(res) == 0 {
		return nil
	}
	finalRes := []ConflictResolution{}
	for _, r := range res {
		finalRes = append(finalRes, ConflictResolution(r))
	}
	return finalRes
}
