// Code generated by nuzur go-code-gen. DO NOT EDIT.

package enums

import (
	"encoding/json"
	"github.com/guregu/null/v6"
	"log"
)

//go:generate go run github.com/dmarkham/enumer -type=SessionOutcome -json
type SessionOutcome int64

const (
	SESSION_OUTCOME_INVALID = iota
	SESSION_OUTCOME_SUCCEEDED
	SESSION_OUTCOME_FAILED
	SESSION_OUTCOME_ABANDONED
)

func (e SessionOutcome) ToInt64() int64 {
	return int64(e)
}

func (e SessionOutcome) ToNullInt() null.Int {
	return null.NewInt(int64(e), true)
}

func SessionOutcomeFromString(in string) SessionOutcome {
	switch in {
	case "invalid":
		return SESSION_OUTCOME_INVALID
	case "succeeded":
		return SESSION_OUTCOME_SUCCEEDED
	case "failed":
		return SESSION_OUTCOME_FAILED
	case "abandoned":
		return SESSION_OUTCOME_ABANDONED
	}
	return SESSION_OUTCOME_INVALID
}

func SessionOutcomeFromPointerString(in *string) SessionOutcome {
	if in == nil {
		return SESSION_OUTCOME_INVALID
	}
	return SessionOutcomeFromString(*in)
}

func (e SessionOutcome) String() string {
	switch e {
	case SESSION_OUTCOME_INVALID:
		return "invalid"
	case SESSION_OUTCOME_SUCCEEDED:
		return "succeeded"
	case SESSION_OUTCOME_FAILED:
		return "failed"
	case SESSION_OUTCOME_ABANDONED:
		return "abandoned"
	}

	return "invalid"
}

func (e SessionOutcome) StringPtr() *string {
	val := e.String()
	return &val
}

func SessionOutcomeSliceToJSON(in []SessionOutcome) json.RawMessage {
	res := make([]int64, len(in))
	for i, e := range in {
		res[i] = int64(e)
	}
	jr, err := json.Marshal(res)
	if err != nil {
		log.Printf("error marshaling SessionOutcome slice to json: %v", err)
		// the empty ARRAY, not a zero-length RawMessage: the latter reaches the
		// driver as an empty string, which no JSON column accepts
		return json.RawMessage("[]")
	}
	return jr
}

func JSONToSessionOutcomeSlice(in []byte) []SessionOutcome {
	res := []int64{}
	// an absent column (NULL / empty / JSON null) is not a decode failure
	if len(in) == 0 || string(in) == "null" {
		return nil
	}
	err := json.Unmarshal(in, &res)
	if err != nil {
		log.Printf("error unmarshaling SessionOutcome slice to int slice: %v", err)
		return nil
	}
	if len(res) == 0 {
		return nil
	}
	finalRes := []SessionOutcome{}
	for _, r := range res {
		finalRes = append(finalRes, SessionOutcome(r))
	}
	return finalRes
}
