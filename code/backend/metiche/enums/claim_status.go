// Code generated by nuzur go-code-gen. DO NOT EDIT.

package enums

import (
	"encoding/json"
	"github.com/guregu/null/v6"
	"log"
)

//go:generate go run github.com/dmarkham/enumer -type=ClaimStatus -json
type ClaimStatus int64

const (
	CLAIM_STATUS_INVALID = iota
	CLAIM_STATUS_HELD
	CLAIM_STATUS_RELEASED
	CLAIM_STATUS_EXPIRED
	CLAIM_STATUS_STALE
	CLAIM_STATUS_REVOKED
)

func (e ClaimStatus) ToInt64() int64 {
	return int64(e)
}

func (e ClaimStatus) ToNullInt() null.Int {
	return null.NewInt(int64(e), true)
}

func ClaimStatusFromString(in string) ClaimStatus {
	switch in {
	case "invalid":
		return CLAIM_STATUS_INVALID
	case "held":
		return CLAIM_STATUS_HELD
	case "released":
		return CLAIM_STATUS_RELEASED
	case "expired":
		return CLAIM_STATUS_EXPIRED
	case "stale":
		return CLAIM_STATUS_STALE
	case "revoked":
		return CLAIM_STATUS_REVOKED
	}
	return CLAIM_STATUS_INVALID
}

func ClaimStatusFromPointerString(in *string) ClaimStatus {
	if in == nil {
		return CLAIM_STATUS_INVALID
	}
	return ClaimStatusFromString(*in)
}

func (e ClaimStatus) String() string {
	switch e {
	case CLAIM_STATUS_INVALID:
		return "invalid"
	case CLAIM_STATUS_HELD:
		return "held"
	case CLAIM_STATUS_RELEASED:
		return "released"
	case CLAIM_STATUS_EXPIRED:
		return "expired"
	case CLAIM_STATUS_STALE:
		return "stale"
	case CLAIM_STATUS_REVOKED:
		return "revoked"
	}

	return "invalid"
}

func (e ClaimStatus) StringPtr() *string {
	val := e.String()
	return &val
}

func ClaimStatusSliceToJSON(in []ClaimStatus) json.RawMessage {
	res := make([]int64, len(in))
	for i, e := range in {
		res[i] = int64(e)
	}
	jr, err := json.Marshal(res)
	if err != nil {
		log.Printf("error marshaling ClaimStatus slice to json: %v", err)
		// the empty ARRAY, not a zero-length RawMessage: the latter reaches the
		// driver as an empty string, which no JSON column accepts
		return json.RawMessage("[]")
	}
	return jr
}

func JSONToClaimStatusSlice(in []byte) []ClaimStatus {
	res := []int64{}
	// an absent column (NULL / empty / JSON null) is not a decode failure
	if len(in) == 0 || string(in) == "null" {
		return nil
	}
	err := json.Unmarshal(in, &res)
	if err != nil {
		log.Printf("error unmarshaling ClaimStatus slice to int slice: %v", err)
		return nil
	}
	if len(res) == 0 {
		return nil
	}
	finalRes := []ClaimStatus{}
	for _, r := range res {
		finalRes = append(finalRes, ClaimStatus(r))
	}
	return finalRes
}
