// Code generated by nuzur go-code-gen. DO NOT EDIT.

package enums

import (
	"encoding/json"
	"github.com/guregu/null/v6"
	"log"
)

//go:generate go run github.com/dmarkham/enumer -type=MemberRole -json
type MemberRole int64

const (
	MEMBER_ROLE_INVALID = iota
	MEMBER_ROLE_OWNER
	MEMBER_ROLE_MEMBER
)

func (e MemberRole) ToInt64() int64 {
	return int64(e)
}

func (e MemberRole) ToNullInt() null.Int {
	return null.NewInt(int64(e), true)
}

func MemberRoleFromString(in string) MemberRole {
	switch in {
	case "invalid":
		return MEMBER_ROLE_INVALID
	case "owner":
		return MEMBER_ROLE_OWNER
	case "member":
		return MEMBER_ROLE_MEMBER
	}
	return MEMBER_ROLE_INVALID
}

func MemberRoleFromPointerString(in *string) MemberRole {
	if in == nil {
		return MEMBER_ROLE_INVALID
	}
	return MemberRoleFromString(*in)
}

func (e MemberRole) String() string {
	switch e {
	case MEMBER_ROLE_INVALID:
		return "invalid"
	case MEMBER_ROLE_OWNER:
		return "owner"
	case MEMBER_ROLE_MEMBER:
		return "member"
	}

	return "invalid"
}

func (e MemberRole) StringPtr() *string {
	val := e.String()
	return &val
}

func MemberRoleSliceToJSON(in []MemberRole) json.RawMessage {
	res := make([]int64, len(in))
	for i, e := range in {
		res[i] = int64(e)
	}
	jr, err := json.Marshal(res)
	if err != nil {
		log.Printf("error marshaling MemberRole slice to json: %v", err)
		// the empty ARRAY, not a zero-length RawMessage: the latter reaches the
		// driver as an empty string, which no JSON column accepts
		return json.RawMessage("[]")
	}
	return jr
}

func JSONToMemberRoleSlice(in []byte) []MemberRole {
	res := []int64{}
	// an absent column (NULL / empty / JSON null) is not a decode failure
	if len(in) == 0 || string(in) == "null" {
		return nil
	}
	err := json.Unmarshal(in, &res)
	if err != nil {
		log.Printf("error unmarshaling MemberRole slice to int slice: %v", err)
		return nil
	}
	if len(res) == 0 {
		return nil
	}
	finalRes := []MemberRole{}
	for _, r := range res {
		finalRes = append(finalRes, MemberRole(r))
	}
	return finalRes
}
