// Code generated by nuzur go-code-gen. DO NOT EDIT.

package enums

import (
	"encoding/json"
	"github.com/guregu/null/v6"
	"log"
)

//go:generate go run github.com/dmarkham/enumer -type=IdentityProvider -json
type IdentityProvider int64

const (
	IDENTITY_PROVIDER_INVALID = iota
	IDENTITY_PROVIDER_NONE
	IDENTITY_PROVIDER_GITHUB
)

func (e IdentityProvider) ToInt64() int64 {
	return int64(e)
}

func (e IdentityProvider) ToNullInt() null.Int {
	return null.NewInt(int64(e), true)
}

func IdentityProviderFromString(in string) IdentityProvider {
	switch in {
	case "invalid":
		return IDENTITY_PROVIDER_INVALID
	case "none":
		return IDENTITY_PROVIDER_NONE
	case "github":
		return IDENTITY_PROVIDER_GITHUB
	}
	return IDENTITY_PROVIDER_INVALID
}

func IdentityProviderFromPointerString(in *string) IdentityProvider {
	if in == nil {
		return IDENTITY_PROVIDER_INVALID
	}
	return IdentityProviderFromString(*in)
}

func (e IdentityProvider) String() string {
	switch e {
	case IDENTITY_PROVIDER_INVALID:
		return "invalid"
	case IDENTITY_PROVIDER_NONE:
		return "none"
	case IDENTITY_PROVIDER_GITHUB:
		return "github"
	}

	return "invalid"
}

func (e IdentityProvider) StringPtr() *string {
	val := e.String()
	return &val
}

func IdentityProviderSliceToJSON(in []IdentityProvider) json.RawMessage {
	res := make([]int64, len(in))
	for i, e := range in {
		res[i] = int64(e)
	}
	jr, err := json.Marshal(res)
	if err != nil {
		log.Printf("error marshaling IdentityProvider slice to json: %v", err)
		// the empty ARRAY, not a zero-length RawMessage: the latter reaches the
		// driver as an empty string, which no JSON column accepts
		return json.RawMessage("[]")
	}
	return jr
}

func JSONToIdentityProviderSlice(in []byte) []IdentityProvider {
	res := []int64{}
	// an absent column (NULL / empty / JSON null) is not a decode failure
	if len(in) == 0 || string(in) == "null" {
		return nil
	}
	err := json.Unmarshal(in, &res)
	if err != nil {
		log.Printf("error unmarshaling IdentityProvider slice to int slice: %v", err)
		return nil
	}
	if len(res) == 0 {
		return nil
	}
	finalRes := []IdentityProvider{}
	for _, r := range res {
		finalRes = append(finalRes, IdentityProvider(r))
	}
	return finalRes
}
