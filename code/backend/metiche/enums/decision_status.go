// Code generated by nuzur go-code-gen. DO NOT EDIT.

package enums

import (
	"encoding/json"
	"github.com/guregu/null/v6"
	"log"
)

//go:generate go run github.com/dmarkham/enumer -type=DecisionStatus -json
type DecisionStatus int64

const (
	DECISION_STATUS_INVALID = iota
	DECISION_STATUS_PROPOSED
	DECISION_STATUS_ACCEPTED
	DECISION_STATUS_SUPERSEDED
	DECISION_STATUS_REVOKED
)

func (e DecisionStatus) ToInt64() int64 {
	return int64(e)
}

func (e DecisionStatus) ToNullInt() null.Int {
	return null.NewInt(int64(e), true)
}

func DecisionStatusFromString(in string) DecisionStatus {
	switch in {
	case "invalid":
		return DECISION_STATUS_INVALID
	case "proposed":
		return DECISION_STATUS_PROPOSED
	case "accepted":
		return DECISION_STATUS_ACCEPTED
	case "superseded":
		return DECISION_STATUS_SUPERSEDED
	case "revoked":
		return DECISION_STATUS_REVOKED
	}
	return DECISION_STATUS_INVALID
}

func DecisionStatusFromPointerString(in *string) DecisionStatus {
	if in == nil {
		return DECISION_STATUS_INVALID
	}
	return DecisionStatusFromString(*in)
}

func (e DecisionStatus) String() string {
	switch e {
	case DECISION_STATUS_INVALID:
		return "invalid"
	case DECISION_STATUS_PROPOSED:
		return "proposed"
	case DECISION_STATUS_ACCEPTED:
		return "accepted"
	case DECISION_STATUS_SUPERSEDED:
		return "superseded"
	case DECISION_STATUS_REVOKED:
		return "revoked"
	}

	return "invalid"
}

func (e DecisionStatus) StringPtr() *string {
	val := e.String()
	return &val
}

func DecisionStatusSliceToJSON(in []DecisionStatus) json.RawMessage {
	res := make([]int64, len(in))
	for i, e := range in {
		res[i] = int64(e)
	}
	jr, err := json.Marshal(res)
	if err != nil {
		log.Printf("error marshaling DecisionStatus slice to json: %v", err)
		// the empty ARRAY, not a zero-length RawMessage: the latter reaches the
		// driver as an empty string, which no JSON column accepts
		return json.RawMessage("[]")
	}
	return jr
}

func JSONToDecisionStatusSlice(in []byte) []DecisionStatus {
	res := []int64{}
	// an absent column (NULL / empty / JSON null) is not a decode failure
	if len(in) == 0 || string(in) == "null" {
		return nil
	}
	err := json.Unmarshal(in, &res)
	if err != nil {
		log.Printf("error unmarshaling DecisionStatus slice to int slice: %v", err)
		return nil
	}
	if len(res) == 0 {
		return nil
	}
	finalRes := []DecisionStatus{}
	for _, r := range res {
		finalRes = append(finalRes, DecisionStatus(r))
	}
	return finalRes
}
