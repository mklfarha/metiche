// Code generated by nuzur go-code-gen. DO NOT EDIT.

package enums

import (
	"encoding/json"
	"github.com/guregu/null/v6"
	"log"
)

//go:generate go run github.com/dmarkham/enumer -type=ClaimMode -json
type ClaimMode int64

const (
	CLAIM_MODE_INVALID = iota
	CLAIM_MODE_READ
	CLAIM_MODE_WRITE
	CLAIM_MODE_STRUCTURAL
)

func (e ClaimMode) ToInt64() int64 {
	return int64(e)
}

func (e ClaimMode) ToNullInt() null.Int {
	return null.NewInt(int64(e), true)
}

func ClaimModeFromString(in string) ClaimMode {
	switch in {
	case "invalid":
		return CLAIM_MODE_INVALID
	case "read":
		return CLAIM_MODE_READ
	case "write":
		return CLAIM_MODE_WRITE
	case "structural":
		return CLAIM_MODE_STRUCTURAL
	}
	return CLAIM_MODE_INVALID
}

func ClaimModeFromPointerString(in *string) ClaimMode {
	if in == nil {
		return CLAIM_MODE_INVALID
	}
	return ClaimModeFromString(*in)
}

func (e ClaimMode) String() string {
	switch e {
	case CLAIM_MODE_INVALID:
		return "invalid"
	case CLAIM_MODE_READ:
		return "read"
	case CLAIM_MODE_WRITE:
		return "write"
	case CLAIM_MODE_STRUCTURAL:
		return "structural"
	}

	return "invalid"
}

func (e ClaimMode) StringPtr() *string {
	val := e.String()
	return &val
}

func ClaimModeSliceToJSON(in []ClaimMode) json.RawMessage {
	res := make([]int64, len(in))
	for i, e := range in {
		res[i] = int64(e)
	}
	jr, err := json.Marshal(res)
	if err != nil {
		log.Printf("error marshaling ClaimMode slice to json: %v", err)
		// the empty ARRAY, not a zero-length RawMessage: the latter reaches the
		// driver as an empty string, which no JSON column accepts
		return json.RawMessage("[]")
	}
	return jr
}

func JSONToClaimModeSlice(in []byte) []ClaimMode {
	res := []int64{}
	// an absent column (NULL / empty / JSON null) is not a decode failure
	if len(in) == 0 || string(in) == "null" {
		return nil
	}
	err := json.Unmarshal(in, &res)
	if err != nil {
		log.Printf("error unmarshaling ClaimMode slice to int slice: %v", err)
		return nil
	}
	if len(res) == 0 {
		return nil
	}
	finalRes := []ClaimMode{}
	for _, r := range res {
		finalRes = append(finalRes, ClaimMode(r))
	}
	return finalRes
}
