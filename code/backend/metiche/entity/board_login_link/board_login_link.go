// Code generated by nuzur go-code-gen. DO NOT EDIT.

package board_login_link

import (
	"encoding/json"
	"log"

	"github.com/gofrs/uuid"
	"github.com/guregu/null/v6"
	"github.com/mklfarha/metiche/backend/enums"
	"time"

	"github.com/mklfarha/metiche/backend/entity/mapper"
)

type BoardLoginLink struct {
	ID           uuid.UUID             `json:"id"`
	AccountUUID  uuid.UUID             `json:"account_uuid"`
	AgentUUID    uuid.UUID             `json:"agent_uuid"`
	SecretHash   string                `json:"secret_hash"`
	RedirectPath string                `json:"redirect_path"`
	RequestedVia enums.BoardLinkSource `json:"requested_via"`
	ExpiresAt    time.Time             `json:"expires_at"`
	ConsumedAt   null.Time             `json:"consumed_at"`
	CreatedAt    time.Time             `json:"created_at"`
	UpdatedAt    time.Time             `json:"updated_at"`
}

func (e BoardLoginLink) String() string {
	res, _ := json.Marshal(e)
	return string(res)
}

func (e BoardLoginLink) PrimaryKeyValues() []string {
	return []string{
		e.ID.String(),
	}
}

// The parameter is a plain []byte rather than json.RawMessage so that both a
// json.RawMessage and the mapper.JSON a repository model holds can be passed
// without a conversion at the call site.
func BoardLoginLinkFromJSON(data []byte) BoardLoginLink {
	entity := BoardLoginLink{}
	if data == nil {
		return entity
	}
	if len(data) == 0 {
		return entity
	}

	if err := json.Unmarshal(data, &entity); err != nil {
		if err2 := mapper.FlexibleUnmarshal(data, &entity); err2 != nil {
			log.Printf("flexible unmarshal error BoardLoginLinkFromJSON: %v\n", err2)
		}
	}
	return entity
}

func BoardLoginLinkSliceFromJSON(data []byte) []BoardLoginLink {
	entity := []BoardLoginLink{}
	if data == nil {
		return entity
	}
	if len(data) == 0 {
		return entity
	}

	if err := json.Unmarshal(data, &entity); err != nil {
		entity = []BoardLoginLink{}
		var rawSlice []json.RawMessage
		if err2 := json.Unmarshal(data, &rawSlice); err2 == nil {
			for _, raw := range rawSlice {
				item := BoardLoginLink{}
				if err3 := mapper.FlexibleUnmarshal(raw, &item); err3 != nil {
					log.Printf("flexible unmarshal error BoardLoginLinkSliceFromJSON item: %v\n", err3)
				}
				entity = append(entity, item)
			}
		}
	}
	return entity
}

func (e BoardLoginLink) ToJSON() json.RawMessage {
	res, err := json.Marshal(e)
	if err != nil {
		log.Printf("marshal error BoardLoginLinkToJSON: %v\n", err)
		return json.RawMessage("null")
	}
	return res
}

func BoardLoginLinkToJSON(e BoardLoginLink) json.RawMessage {
	res, err := json.Marshal(e)
	if err != nil {
		log.Printf("marshal error BoardLoginLinkToJSON: %v\n", err)
		return json.RawMessage("null")
	}
	return res
}

// BoardLoginLinkSliceToJSON serializes a ONE_TO_MANY embed for its JSON column.
// An absent embed is the EMPTY ARRAY, never a zero-length json.RawMessage: a
// zero-length value reaches the driver as the empty string, which is not valid
// JSON, so a create that simply omitted an optional array embed was rejected by
// the database ("invalid input syntax for type json", MySQL error 3140) and
// surfaced as a 500. "[]" is also exactly what an explicit `"field": []` stores,
// so the omitted and the explicitly-empty request round-trip to the same value.
func BoardLoginLinkSliceToJSON(e []BoardLoginLink) json.RawMessage {
	if e == nil {
		return json.RawMessage("[]")
	}
	res, err := json.Marshal(e)
	if err != nil {
		log.Printf("marshal error BoardLoginLinkSliceToJSON: %v\n", err)
		return json.RawMessage("[]")
	}
	return res
}
