// Code generated by nuzur go-code-gen. DO NOT EDIT.

package contract_field

import (
	"encoding/json"
	"log"

	"github.com/gofrs/uuid"
	"github.com/mklfarha/metiche/backend/enums"
	"time"

	"github.com/mklfarha/metiche/backend/entity/mapper"
)

type ContractField struct {
	ID            uuid.UUID               `json:"id"`
	AssertionUUID uuid.UUID               `json:"assertion_uuid"`
	ContractUUID  uuid.UUID               `json:"contract_uuid"`
	Path          string                  `json:"path"`
	PathSnake     string                  `json:"path_snake"`
	Type          enums.ContractFieldType `json:"type"`
	Required      bool                    `json:"required"`
	Nullable      bool                    `json:"nullable"`
	Direction     enums.FieldDirection    `json:"direction"`
	CreatedAt     time.Time               `json:"created_at"`
	UpdatedAt     time.Time               `json:"updated_at"`
}

func (e ContractField) String() string {
	res, _ := json.Marshal(e)
	return string(res)
}

func (e ContractField) PrimaryKeyValues() []string {
	return []string{
		e.ID.String(),
	}
}

// The parameter is a plain []byte rather than json.RawMessage so that both a
// json.RawMessage and the mapper.JSON a repository model holds can be passed
// without a conversion at the call site.
func ContractFieldFromJSON(data []byte) ContractField {
	entity := ContractField{}
	if data == nil {
		return entity
	}
	if len(data) == 0 {
		return entity
	}

	if err := json.Unmarshal(data, &entity); err != nil {
		if err2 := mapper.FlexibleUnmarshal(data, &entity); err2 != nil {
			log.Printf("flexible unmarshal error ContractFieldFromJSON: %v\n", err2)
		}
	}
	return entity
}

func ContractFieldSliceFromJSON(data []byte) []ContractField {
	entity := []ContractField{}
	if data == nil {
		return entity
	}
	if len(data) == 0 {
		return entity
	}

	if err := json.Unmarshal(data, &entity); err != nil {
		entity = []ContractField{}
		var rawSlice []json.RawMessage
		if err2 := json.Unmarshal(data, &rawSlice); err2 == nil {
			for _, raw := range rawSlice {
				item := ContractField{}
				if err3 := mapper.FlexibleUnmarshal(raw, &item); err3 != nil {
					log.Printf("flexible unmarshal error ContractFieldSliceFromJSON item: %v\n", err3)
				}
				entity = append(entity, item)
			}
		}
	}
	return entity
}

func (e ContractField) ToJSON() json.RawMessage {
	res, err := json.Marshal(e)
	if err != nil {
		log.Printf("marshal error ContractFieldToJSON: %v\n", err)
		return json.RawMessage("null")
	}
	return res
}

func ContractFieldToJSON(e ContractField) json.RawMessage {
	res, err := json.Marshal(e)
	if err != nil {
		log.Printf("marshal error ContractFieldToJSON: %v\n", err)
		return json.RawMessage("null")
	}
	return res
}

// ContractFieldSliceToJSON serializes a ONE_TO_MANY embed for its JSON column.
// An absent embed is the EMPTY ARRAY, never a zero-length json.RawMessage: a
// zero-length value reaches the driver as the empty string, which is not valid
// JSON, so a create that simply omitted an optional array embed was rejected by
// the database ("invalid input syntax for type json", MySQL error 3140) and
// surfaced as a 500. "[]" is also exactly what an explicit `"field": []` stores,
// so the omitted and the explicitly-empty request round-trip to the same value.
func ContractFieldSliceToJSON(e []ContractField) json.RawMessage {
	if e == nil {
		return json.RawMessage("[]")
	}
	res, err := json.Marshal(e)
	if err != nil {
		log.Printf("marshal error ContractFieldSliceToJSON: %v\n", err)
		return json.RawMessage("[]")
	}
	return res
}
