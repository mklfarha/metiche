// Code generated by nuzur go-code-gen. DO NOT EDIT.

package project

import (
	"github.com/mklfarha/metiche/backend/entity/validation"
)

// Validate checks this Project against the schema's field type and
// type-config rules, aggregating every problem into a single *validation.Error.
// It mirrors the change-request data validation enforced in
// product/module/datavalidation, so data written through this API is held to
// the same rules. Generated by go-code-gen — do not edit.
func (e Project) Validate() error {
	c := validation.NewCollector()
	if e.ID.IsNil() {
		c.Require("project.id")
	}
	if e.TeamUUID.IsNil() {
		c.Require("project.team_uuid")
	}
	if e.Key == "" {
		c.Require("project.key")
	} else {
		c.Field("project.key", validation.String(e.Key, 0, 0, ""))

	}
	if e.Name == "" {
		c.Require("project.name")
	} else {
		c.Field("project.name", validation.String(e.Name, 0, 120, ""))

	}
	if e.RepoURL.Valid && e.RepoURL.String != "" {
		c.Field("project.repo_url", validation.URL(e.RepoURL.String, false, nil, nil, nil))

	}
	if e.DefaultBranch.Valid && e.DefaultBranch.String != "" {
		c.Field("project.default_branch", validation.String(e.DefaultBranch.String, 0, 120, ""))

	}
	if len(e.IgnorePatterns) > 0 {
		for i, el := range e.IgnorePatterns {
			c.Field(validation.Index("project.ignore_patterns", i), validation.String(el, 0, 0, ""))
		}

	}
	if len(e.HotspotPatterns) > 0 {
		for i, el := range e.HotspotPatterns {
			c.Field(validation.Index("project.hotspot_patterns", i), validation.String(el, 0, 0, ""))
		}

	}
	if e.Status.ToInt64() == 0 {
		c.Require("project.status")
	} else {
		c.Field("project.status", validation.EnumMember(e.Status.ToInt64(), []int64{0, 1, 2}, "record_status"))

	}
	if e.Cadence.ToInt64() == 0 {
		c.Require("project.cadence")
	} else {
		c.Field("project.cadence", validation.EnumMember(e.Cadence.ToInt64(), []int64{0, 1, 2, 3}, "project_cadence"))

	}

	return c.Result()
}
