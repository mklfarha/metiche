// Code generated by nuzur go-code-gen. DO NOT EDIT.

package contract

import (
	"github.com/mklfarha/metiche/backend/entity/validation"
)

// Validate checks this Contract against the schema's field type and
// type-config rules, aggregating every problem into a single *validation.Error.
// It mirrors the change-request data validation enforced in
// product/module/datavalidation, so data written through this API is held to
// the same rules. Generated by go-code-gen — do not edit.
func (e Contract) Validate() error {
	c := validation.NewCollector()
	if e.ID.IsNil() {
		c.Require("contract.id")
	}
	if e.TeamUUID.IsNil() {
		c.Require("contract.team_uuid")
	}
	if e.ProjectUUID.IsNil() {
		c.Require("contract.project_uuid")
	}
	if e.Key == "" {
		c.Require("contract.key")
	} else {
		c.Field("contract.key", validation.String(e.Key, 0, 255, ""))

	}
	if e.KeyNorm == "" {
		c.Require("contract.key_norm")
	} else {
		c.Field("contract.key_norm", validation.String(e.KeyNorm, 0, 255, ""))

	}
	if e.Kind.ToInt64() == 0 {
		c.Require("contract.kind")
	} else {
		c.Field("contract.kind", validation.EnumMember(e.Kind.ToInt64(), []int64{0, 1, 2, 3, 4, 5, 6, 7, 8}, "contract_kind"))

	}
	if e.Status.ToInt64() == 0 {
		c.Require("contract.status")
	} else {
		c.Field("contract.status", validation.EnumMember(e.Status.ToInt64(), []int64{0, 1, 2, 3, 4}, "contract_status"))

	}
	if e.Title.Valid && e.Title.String != "" {
		c.Field("contract.title", validation.String(e.Title.String, 0, 200, ""))

	}

	return c.Result()
}
