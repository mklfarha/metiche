// Code generated by nuzur go-code-gen. DO NOT EDIT.

package invite

import (
	"encoding/json"
	"log"

	"github.com/gofrs/uuid"
	"github.com/guregu/null/v6"
	"github.com/mklfarha/metiche/backend/enums"
	"time"

	"github.com/mklfarha/metiche/backend/entity/mapper"
)

type Invite struct {
	ID                  uuid.UUID          `json:"id"`
	TeamUUID            uuid.UUID          `json:"team_uuid"`
	Code                string             `json:"code"`
	Label               null.String        `json:"label"`
	CreatedByMemberUUID *uuid.UUID         `json:"created_by_member_uuid"`
	MaxUses             null.Int64         `json:"max_uses"`
	Uses                int64              `json:"uses"`
	ExpiresAt           null.Time          `json:"expires_at"`
	RevokedAt           null.Time          `json:"revoked_at"`
	RevokedByMemberUUID *uuid.UUID         `json:"revoked_by_member_uuid"`
	Status              enums.InviteStatus `json:"status"`
	LastUsedAt          null.Time          `json:"last_used_at"`
	CreatedAt           time.Time          `json:"created_at"`
	UpdatedAt           time.Time          `json:"updated_at"`
}

func (e Invite) String() string {
	res, _ := json.Marshal(e)
	return string(res)
}

func (e Invite) PrimaryKeyValues() []string {
	return []string{
		e.ID.String(),
	}
}

// The parameter is a plain []byte rather than json.RawMessage so that both a
// json.RawMessage and the mapper.JSON a repository model holds can be passed
// without a conversion at the call site.
func InviteFromJSON(data []byte) Invite {
	entity := Invite{}
	if data == nil {
		return entity
	}
	if len(data) == 0 {
		return entity
	}

	if err := json.Unmarshal(data, &entity); err != nil {
		if err2 := mapper.FlexibleUnmarshal(data, &entity); err2 != nil {
			log.Printf("flexible unmarshal error InviteFromJSON: %v\n", err2)
		}
	}
	return entity
}

func InviteSliceFromJSON(data []byte) []Invite {
	entity := []Invite{}
	if data == nil {
		return entity
	}
	if len(data) == 0 {
		return entity
	}

	if err := json.Unmarshal(data, &entity); err != nil {
		entity = []Invite{}
		var rawSlice []json.RawMessage
		if err2 := json.Unmarshal(data, &rawSlice); err2 == nil {
			for _, raw := range rawSlice {
				item := Invite{}
				if err3 := mapper.FlexibleUnmarshal(raw, &item); err3 != nil {
					log.Printf("flexible unmarshal error InviteSliceFromJSON item: %v\n", err3)
				}
				entity = append(entity, item)
			}
		}
	}
	return entity
}

func (e Invite) ToJSON() json.RawMessage {
	res, err := json.Marshal(e)
	if err != nil {
		log.Printf("marshal error InviteToJSON: %v\n", err)
		return json.RawMessage("null")
	}
	return res
}

func InviteToJSON(e Invite) json.RawMessage {
	res, err := json.Marshal(e)
	if err != nil {
		log.Printf("marshal error InviteToJSON: %v\n", err)
		return json.RawMessage("null")
	}
	return res
}

// InviteSliceToJSON serializes a ONE_TO_MANY embed for its JSON column.
// An absent embed is the EMPTY ARRAY, never a zero-length json.RawMessage: a
// zero-length value reaches the driver as the empty string, which is not valid
// JSON, so a create that simply omitted an optional array embed was rejected by
// the database ("invalid input syntax for type json", MySQL error 3140) and
// surfaced as a 500. "[]" is also exactly what an explicit `"field": []` stores,
// so the omitted and the explicitly-empty request round-trip to the same value.
func InviteSliceToJSON(e []Invite) json.RawMessage {
	if e == nil {
		return json.RawMessage("[]")
	}
	res, err := json.Marshal(e)
	if err != nil {
		log.Printf("marshal error InviteSliceToJSON: %v\n", err)
		return json.RawMessage("[]")
	}
	return res
}
