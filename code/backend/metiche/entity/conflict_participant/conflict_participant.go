// Code generated by nuzur go-code-gen. DO NOT EDIT.

package conflict_participant

import (
	"encoding/json"
	"log"

	"github.com/gofrs/uuid"
	"github.com/guregu/null/v6"
	"github.com/mklfarha/metiche/backend/enums"
	"time"

	"github.com/mklfarha/metiche/backend/entity/mapper"
)

type ConflictParticipant struct {
	ID           uuid.UUID                 `json:"id"`
	ConflictUUID uuid.UUID                 `json:"conflict_uuid"`
	TeamUUID     uuid.UUID                 `json:"team_uuid"`
	SessionUUID  uuid.UUID                 `json:"session_uuid"`
	AgentUUID    uuid.UUID                 `json:"agent_uuid"`
	MemberUUID   uuid.UUID                 `json:"member_uuid"`
	SubjectKind  enums.SubjectKind         `json:"subject_kind"`
	SubjectUUID  uuid.UUID                 `json:"subject_uuid"`
	Role         enums.ParticipantRole     `json:"role"`
	NotifiedAt   null.Time                 `json:"notified_at"`
	AckedAt      null.Time                 `json:"acked_at"`
	Response     enums.ParticipantResponse `json:"response"`
	CreatedAt    time.Time                 `json:"created_at"`
	UpdatedAt    time.Time                 `json:"updated_at"`
}

func (e ConflictParticipant) String() string {
	res, _ := json.Marshal(e)
	return string(res)
}

func (e ConflictParticipant) PrimaryKeyValues() []string {
	return []string{
		e.ID.String(),
	}
}

// The parameter is a plain []byte rather than json.RawMessage so that both a
// json.RawMessage and the mapper.JSON a repository model holds can be passed
// without a conversion at the call site.
func ConflictParticipantFromJSON(data []byte) ConflictParticipant {
	entity := ConflictParticipant{}
	if data == nil {
		return entity
	}
	if len(data) == 0 {
		return entity
	}

	if err := json.Unmarshal(data, &entity); err != nil {
		if err2 := mapper.FlexibleUnmarshal(data, &entity); err2 != nil {
			log.Printf("flexible unmarshal error ConflictParticipantFromJSON: %v\n", err2)
		}
	}
	return entity
}

func ConflictParticipantSliceFromJSON(data []byte) []ConflictParticipant {
	entity := []ConflictParticipant{}
	if data == nil {
		return entity
	}
	if len(data) == 0 {
		return entity
	}

	if err := json.Unmarshal(data, &entity); err != nil {
		entity = []ConflictParticipant{}
		var rawSlice []json.RawMessage
		if err2 := json.Unmarshal(data, &rawSlice); err2 == nil {
			for _, raw := range rawSlice {
				item := ConflictParticipant{}
				if err3 := mapper.FlexibleUnmarshal(raw, &item); err3 != nil {
					log.Printf("flexible unmarshal error ConflictParticipantSliceFromJSON item: %v\n", err3)
				}
				entity = append(entity, item)
			}
		}
	}
	return entity
}

func (e ConflictParticipant) ToJSON() json.RawMessage {
	res, err := json.Marshal(e)
	if err != nil {
		log.Printf("marshal error ConflictParticipantToJSON: %v\n", err)
		return json.RawMessage("null")
	}
	return res
}

func ConflictParticipantToJSON(e ConflictParticipant) json.RawMessage {
	res, err := json.Marshal(e)
	if err != nil {
		log.Printf("marshal error ConflictParticipantToJSON: %v\n", err)
		return json.RawMessage("null")
	}
	return res
}

// ConflictParticipantSliceToJSON serializes a ONE_TO_MANY embed for its JSON column.
// An absent embed is the EMPTY ARRAY, never a zero-length json.RawMessage: a
// zero-length value reaches the driver as the empty string, which is not valid
// JSON, so a create that simply omitted an optional array embed was rejected by
// the database ("invalid input syntax for type json", MySQL error 3140) and
// surfaced as a 500. "[]" is also exactly what an explicit `"field": []` stores,
// so the omitted and the explicitly-empty request round-trip to the same value.
func ConflictParticipantSliceToJSON(e []ConflictParticipant) json.RawMessage {
	if e == nil {
		return json.RawMessage("[]")
	}
	res, err := json.Marshal(e)
	if err != nil {
		log.Printf("marshal error ConflictParticipantSliceToJSON: %v\n", err)
		return json.RawMessage("[]")
	}
	return res
}
