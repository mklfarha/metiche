// Code generated by nuzur go-code-gen. DO NOT EDIT.

package decision_path

import (
	"github.com/mklfarha/metiche/backend/entity/validation"
)

// Validate checks this DecisionPath against the schema's field type and
// type-config rules, aggregating every problem into a single *validation.Error.
// It mirrors the change-request data validation enforced in
// product/module/datavalidation, so data written through this API is held to
// the same rules. Generated by go-code-gen — do not edit.
func (e DecisionPath) Validate() error {
	c := validation.NewCollector()
	if e.ID.IsNil() {
		c.Require("decision_path.id")
	}
	if e.DecisionUUID.IsNil() {
		c.Require("decision_path.decision_uuid")
	}
	if e.TeamUUID.IsNil() {
		c.Require("decision_path.team_uuid")
	}
	if e.Pattern == "" {
		c.Require("decision_path.pattern")
	} else {
		c.Field("decision_path.pattern", validation.String(e.Pattern, 0, 400, ""))

	}
	if e.PatternNorm == "" {
		c.Require("decision_path.pattern_norm")
	} else {
		c.Field("decision_path.pattern_norm", validation.String(e.PatternNorm, 0, 400, ""))

	}
	if e.Kind.ToInt64() == 0 {
		c.Require("decision_path.kind")
	} else {
		c.Field("decision_path.kind", validation.EnumMember(e.Kind.ToInt64(), []int64{0, 1, 2, 3}, "claim_path_kind"))

	}
	if e.Prefix == "" {
		c.Require("decision_path.prefix")
	} else {
		c.Field("decision_path.prefix", validation.String(e.Prefix, 0, 400, ""))

	}
	c.Field("decision_path.depth", validation.Integer(e.Depth, false, 0, 0, false, false, false, true, -32768, 32767))

	return c.Result()
}
