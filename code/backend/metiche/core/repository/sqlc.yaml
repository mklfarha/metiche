# Code generated by nuzur go-code-gen. DO NOT EDIT.
version: "2"
sql:
  - engine: "mysql"
    schema: "sql/schema/create.sql"
    queries: "sql/queries/"
    gen:
      go:
        emit_exact_table_names: true
        package: "metichedb"
        out: "gen"
        emit_interface: true
        emit_json_tags: true
        rename: 
          a_label: ALabel
          a_pattern: APattern
          a_summary: ASummary
          account_uuid: AccountUUID
          acked_at: AckedAt
          acted_at: ActedAt
          action: Action
          action_note: ActionNote
          active_marker: ActiveMarker
          adjusters: Adjusters
          agent_uuid: AgentUUID
          allowed: Allowed
          always_show: AlwaysShow
          asserted_at: AssertedAt
          assertion_uuid: AssertionUUID
          b_label: BLabel
          b_pattern: BPattern
          b_summary: BSummary
          base_commit: BaseCommit
          board_revision: BoardRevision
          body: Body
          branch: Branch
          breadth_score: BreadthScore
          cadence: Cadence
          case_insensitive_paths: CaseInsensitivePaths
          claim_ttl_seconds: ClaimTtlSeconds
          claim_uuid: ClaimUUID
          claimed_at: ClaimedAt
          client_key: ClientKey
          client_kind: ClientKind
          code: Code
          confidence: Confidence
          conflict_uuid: ConflictUUID
          consecutive_failures: ConsecutiveFailures
          contract_uuid: ContractUUID
          created_at: CreatedAt
          created_by_member_uuid: CreatedByMemberUUID
          current_intent_uuid: CurrentIntentUUID
          decided_at: DecidedAt
          decided_by_member_uuid: DecidedByMemberUUID
          decision_uuid: DecisionUUID
          declared_at: DeclaredAt
          dedupe_key: DedupeKey
          default_branch: DefaultBranch
          delivered_at: DeliveredAt
          delivery_status: DeliveryStatus
          demoted_rules: DemotedRules
          depth: Depth
          description: Description
          detail: Detail
          detected_by: DetectedBy
          detector_rule: DetectorRule
          dimension: Dimension
          direction: Direction
          dismiss_reason: DismissReason
          display_name: DisplayName
          email: Email
          ended_at: EndedAt
          evidence: Evidence
          expires_at: ExpiresAt
          ext: Ext
          external_ref: ExternalRef
          field_issues: FieldIssues
          first_detected_at: FirstDetectedAt
          goal: Goal
          hard_expires_at: HardExpiresAt
          head_commit: HeadCommit
          hotspot_patterns: HotspotPatterns
          human_notify_floor: HumanNotifyFloor
          id: ID
          idempotency_key: IdempotencyKey
          identity_handle: IdentityHandle
          identity_provider: IdentityProvider
          identity_subject: IdentitySubject
          ignore_patterns: IgnorePatterns
          instruction_uuid: InstructionUUID
          intent_uuid: IntentUUID
          is_instance_default: IsInstanceDefault
          judge_session_uuid: JudgeSessionUUID
          judge_window_seconds: JudgeWindowSeconds
          judging_expires_at: JudgingExpiresAt
          key: Key
          key_norm: KeyNorm
          kind: Kind
          label: Label
          last_attempt_at: LastAttemptAt
          last_detected_at: LastDetectedAt
          last_error: LastError
          last_heartbeat_at: LastHeartbeatAt
          last_retention_sweep_at: LastRetentionSweepAt
          last_seen_at: LastSeenAt
          last_success_at: LastSuccessAt
          last_used_at: LastUsedAt
          max_concurrent_agents: MaxConcurrentAgents
          max_members: MaxMembers
          max_projects: MaxProjects
          max_reviews_per_minute: MaxReviewsPerMinute
          max_severity_notified: MaxSeverityNotified
          max_uses: MaxUses
          member_uuid: MemberUUID
          message: Message
          min_severity: MinSeverity
          mode: Mode
          name: Name
          new_status: NewStatus
          note: Note
          notes: Notes
          notified_at: NotifiedAt
          notify_floor: NotifyFloor
          notify_human_requests: NotifyHumanRequests
          nullable: Nullable
          observed: Observed
          occurred_at: OccurredAt
          occurrence_count: OccurrenceCount
          outcome: Outcome
          outcome_note: OutcomeNote
          overlap_path: OverlapPath
          pair_key: PairKey
          path: Path
          path_snake: PathSnake
          paths: Paths
          pattern: Pattern
          pattern_norm: PatternNorm
          payload: Payload
          pinned: Pinned
          plan_expires_at: PlanExpiresAt
          plan_granted_reason: PlanGrantedReason
          plan_source: PlanSource
          plan_uuid: PlanUUID
          prefix: Prefix
          previous_status: PreviousStatus
          project_uuid: ProjectUUID
          raised_by_member_uuid: RaisedByMemberUUID
          rationale: Rationale
          ref_kind: RefKind
          ref_uuid: RefUUID
          release_reason: ReleaseReason
          released_at: ReleasedAt
          repo_url: RepoURL
          required: Required
          requires_claimed_accounts: RequiresClaimedAccounts
          requires_report: RequiresReport
          resolution: Resolution
          resolution_note: ResolutionNote
          resolved_at: ResolvedAt
          resolved_by_member_uuid: ResolvedByMemberUUID
          response: Response
          response_snapshot: ResponseSnapshot
          retention_days: RetentionDays
          retention_floor_sequence: RetentionFloorSequence
          review_block_enabled: ReviewBlockEnabled
          revision: Revision
          revoked_at: RevokedAt
          revoked_by_member_uuid: RevokedByMemberUUID
          role: Role
          sequence: Sequence
          session_abandoned_seconds: SessionAbandonedSeconds
          session_stale_seconds: SessionStaleSeconds
          session_uuid: SessionUUID
          settings: Settings
          severity: Severity
          shape: Shape
          shape_fingerprint: ShapeFingerprint
          shape_hash: ShapeHash
          slug: Slug
          sort_order: SortOrder
          source: Source
          started_at: StartedAt
          statement: Statement
          status: Status
          status_line: StatusLine
          structural: Structural
          subject_a_kind: SubjectAKind
          subject_a_revision: SubjectARevision
          subject_a_uuid: SubjectAUUID
          subject_b_kind: SubjectBKind
          subject_b_revision: SubjectBRevision
          subject_b_uuid: SubjectBUUID
          subject_key: SubjectKey
          subject_kind: SubjectKind
          subject_uuid: SubjectUUID
          suffix_pattern: SuffixPattern
          suggested_action: SuggestedAction
          suggested_yield_reason: SuggestedYieldReason
          suggested_yield_session_uuid: SuggestedYieldSessionUUID
          summary: Summary
          superseded_by_uuid: SupersededByUUID
          supersedes_uuid: SupersedesUUID
          target_agent_uuid: TargetAgentUUID
          target_member_uuid: TargetMemberUUID
          target_session_uuid: TargetSessionUUID
          target_url: TargetURL
          team_uuid: TeamUUID
          title: Title
          token: Token
          token_hash: TokenHash
          ttl_seconds: TtlSeconds
          type: Type
          updated_at: UpdatedAt
          uses: Uses
          verdict: Verdict
          visibility: Visibility
          weight: Weight        
        # sqlc type overrides. Keys are the db_type strings sqlc derives from the
        # schema, which DIFFER by engine (MySQL bare names vs Postgres catalog
        # names like pg_catalog.varchar / bpchar / pg_catalog.timestamp). Each
        # override is made dialect-aware so Postgres sqlc emits the SAME Go types
        # MySQL does (string UUIDs, guregu null.*, []byte json) — that keeps the
        # domain layer, the mappers, and every repo call site engine-agnostic.
        overrides:
          - db_type: "int"
            go_type:
              type: "int64"
          # NOT []byte: go-sql-driver/mysql renders a []byte parameter as a
          # _binary'...' literal when the DSN sets interpolateParams=true, and
          # MySQL refuses to cast a binary-charset string to JSON (error 3144),
          # so every write to a JSON column failed on such a DSN. mapper.JSON is
          # a driver.Valuer that hands the driver a string, and a sql.Scanner so
          # a NULL column still reads back — which plain json.RawMessage cannot
          # do (database/sql cannot scan NULL into a bare named slice type, the
          # "sqlc bug" the []byte override was working around).
          - db_type: "json"
            go_type:
              import: "github.com/mklfarha/metiche/backend/entity/mapper"
              package: "mapper"
              type: "JSON"
          - db_type: "json"
            nullable: true
            go_type:
              import: "github.com/mklfarha/metiche/backend/entity/mapper"
              package: "mapper"
              type: "JSON"
          - db_type: "datetime"
            nullable: true
            go_type:
              import: "github.com/guregu/null/v6"
              package: "null"
              type: "Time"
          - db_type: "date"
            nullable: true
            go_type:
              import: "github.com/guregu/null/v6"
              package: "null"
              type: "Time"
          - db_type: "time"
            nullable: true
            go_type:
              import: "github.com/guregu/null/v6"
              package: "null"
              type: "Time"
          - db_type: "varchar"
            nullable: true
            go_type:
              import: "github.com/guregu/null/v6"
              package: "null"
              type: "String"
          - db_type: "char"
            nullable: true
            go_type:
              import: "github.com/guregu/null/v6"
              package: "null"
              type: "String"
          - db_type: "text"
            nullable: true
            go_type:
              import: "github.com/guregu/null/v6"
              package: "null"
              type: "String"
          # MySQL tiers TEXT by max_size (TINYTEXT / TEXT / MEDIUMTEXT / LONGTEXT);
          # all four are plain string / null.String in the domain layer. Postgres
          # has a single TEXT type, so it needs no extra entries here.
          - db_type: "tinytext"
            nullable: true
            go_type:
              import: "github.com/guregu/null/v6"
              package: "null"
              type: "String"
          - db_type: "mediumtext"
            nullable: true
            go_type:
              import: "github.com/guregu/null/v6"
              package: "null"
              type: "String"
          - db_type: "longtext"
            nullable: true
            go_type:
              import: "github.com/guregu/null/v6"
              package: "null"
              type: "String"
          - db_type: "int"
            nullable: true
            go_type:
              import: "github.com/guregu/null/v6"
              package: "null"
              type: "Int"
          # An INTEGER field's size picks a different SQL width (MySQL SMALLINT /
          # MEDIUMINT / BIGINT, Postgres int2 / int8), but GolangType() emits
          # int64 / null.Int for EVERY width and the upsert path passes the entity
          # value straight through — so each width needs the same override as int.
          - db_type: "bigint"
            go_type:
              type: "int64"
          - db_type: "bigint"
            nullable: true
            go_type:
              import: "github.com/guregu/null/v6"
              package: "null"
              type: "Int"
          - db_type: "mediumint"
            go_type:
              type: "int64"
          - db_type: "mediumint"
            nullable: true
            go_type:
              import: "github.com/guregu/null/v6"
              package: "null"
              type: "Int"
          - db_type: "smallint"
            go_type:
              type: "int64"
          - db_type: "smallint"
            nullable: true
            go_type:
              import: "github.com/guregu/null/v6"
              package: "null"
              type: "Int"
          - db_type: "float"
            nullable: true
            go_type:
              import: "github.com/guregu/null/v6"
              package: "null"
              type: "Float"
          - db_type: "double"
            nullable: true
            go_type:
              import: "github.com/guregu/null/v6"
              package: "null"
              type: "Float"
          # DECIMAL has no sqlc default that matches the domain layer: MySQL
          # `decimal` and Postgres `numeric` both fall back to string /
          # sql.NullString, while GolangType() emits float64 / null.Float and the
          # upsert path passes the entity value through untouched. BOTH
          # nullabilities need an override — miss either and any decimal field
          # generates code that does not compile.
          - db_type: "decimal"
            go_type:
              type: "float64"
          - db_type: "decimal"
            nullable: true
            go_type:
              import: "github.com/guregu/null/v6"
              package: "null"
              type: "Float"
          - db_type: "tinyint"
            nullable: true
            go_type:
              import: "github.com/guregu/null/v6"
              package: "null"
              type: "Bool"
          # A file/image/audio/video field with BINARY storage is []byte in the
          # domain layer for both nullabilities. sqlc's default for a NULLABLE
          # blob is sql.NullString, which neither the entity struct ([]byte) nor
          # the mapper can use — the non-null case needs no override, the
          # nullable one does. MySQL tiers BLOB by max_size the same way it
          # tiers TEXT, so every width needs the entry.
          - db_type: "blob"
            nullable: true
            go_type:
              type: "[]byte"
          - db_type: "tinyblob"
            nullable: true
            go_type:
              type: "[]byte"
          - db_type: "mediumblob"
            nullable: true
            go_type:
              type: "[]byte"
          - db_type: "longblob"
            nullable: true
            go_type:
              type: "[]byte"
overrides:
  go: 
    rename:
      account: Account
      agent: Agent
      claim: Claim
      claim_path: ClaimPath
      conflict: Conflict
      conflict_evidence: ConflictEvidence
      conflict_participant: ConflictParticipant
      contract: Contract
      contract_assertion: ContractAssertion
      contract_field: ContractField
      decision: Decision
      decision_path: DecisionPath
      decision_token: DecisionToken
      event_payload: EventPayload
      instruction: Instruction
      intent: Intent
      intent_token: IntentToken
      invite: Invite
      judgement: Judgement
      limit_event: LimitEvent
      member: Member
      notification_channel: NotificationChannel
      plan: Plan
      project: Project
      session: Session
      team: Team
      team_event: TeamEvent
      team_settings: TeamSettings