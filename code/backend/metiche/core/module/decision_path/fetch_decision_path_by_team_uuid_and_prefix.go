// Code generated by nuzur go-code-gen. DO NOT EDIT.

package decision_path

import (
	"context"
	"fmt"

	"github.com/mklfarha/metiche/backend/core/module/decision_path/types"

	"errors"
	metichedb "github.com/mklfarha/metiche/backend/core/repository/gen"
)

func (m *module) FetchDecisionPathByTeamUUIDAndPrefix(
	ctx context.Context,
	req types.FetchDecisionPathByTeamUUIDAndPrefixRequest,
	opts ...Option,
) (types.FetchDecisionPathByTeamUUIDAndPrefixResponse, error) {

	resolvedOpts := applyAllOptions(opts)
	queries := m.repository.Queries
	if resolvedOpts.SQLTx != nil {
		queries = queries.WithTx(resolvedOpts.SQLTx)
	}
	// A read inside a transaction must observe that transaction's own
	// uncommitted writes, so it can neither be served from the shared cache nor
	// be collapsed into another caller's in-flight query.
	skipShared := resolvedOpts.SkipCache || resolvedOpts.SQLTx != nil
	cacheKey := fmt.Sprintf("FetchDecisionPathByTeamUUIDAndPrefix:%v", req)
	if !skipShared {
		if cached, found := m.cache.Get(cacheKey); found {
			return cached.(types.FetchDecisionPathByTeamUUIDAndPrefixResponse), nil
		}
	}
	fetch := func() (any, error) {
		if req.OrderBy == "" {
			models, err := queries.FetchDecisionPathByTeamUUIDAndPrefix(
				ctx,
				metichedb.FetchDecisionPathByTeamUUIDAndPrefixParams{
					TeamUUID: req.TeamUUID.String(),

					Prefix: req.Prefix,

					Offset: req.Offset,
					Limit:  req.Limit,
				},
			)

			if err != nil {

				return types.FetchDecisionPathByTeamUUIDAndPrefixResponse{}, err
			}
			return types.FetchDecisionPathByTeamUUIDAndPrefixResponse{
				Results: mapModelsToEntities(models),
			}, nil
		}

		err := errors.New("could not process request")

		return types.FetchDecisionPathByTeamUUIDAndPrefixResponse{}, err
	} // end fetch
	var v any
	var fetchErr error
	if skipShared {
		v, fetchErr = fetch()
	} else {
		v, fetchErr, _ = m.sg.Do(cacheKey, fetch)
	}
	if fetchErr != nil {
		return types.FetchDecisionPathByTeamUUIDAndPrefixResponse{}, fetchErr
	}
	result := v.(types.FetchDecisionPathByTeamUUIDAndPrefixResponse)
	if !skipShared {
		m.cache.Set(cacheKey, result, 0)
	}
	return result, nil

}
