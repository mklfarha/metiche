// Code generated by nuzur go-code-gen. DO NOT EDIT.

package conflict

import (
	"context"
	"fmt"

	"github.com/mklfarha/metiche/backend/core/module/conflict/types"

	"errors"
	metichedb "github.com/mklfarha/metiche/backend/core/repository/gen"
)

func (m *module) FetchConflictByTeamUUIDAndStatusAndSeverity(
	ctx context.Context,
	req types.FetchConflictByTeamUUIDAndStatusAndSeverityRequest,
	opts ...Option,
) (types.FetchConflictByTeamUUIDAndStatusAndSeverityResponse, error) {

	resolvedOpts := applyAllOptions(opts)
	queries := m.repository.Queries
	if resolvedOpts.SQLTx != nil {
		queries = queries.WithTx(resolvedOpts.SQLTx)
	}
	// A read inside a transaction must observe that transaction's own
	// uncommitted writes, so it can neither be served from the shared cache nor
	// be collapsed into another caller's in-flight query.
	skipShared := resolvedOpts.SkipCache || resolvedOpts.SQLTx != nil
	cacheKey := fmt.Sprintf("FetchConflictByTeamUUIDAndStatusAndSeverity:%v", req)
	if !skipShared {
		if cached, found := m.cache.Get(cacheKey); found {
			return cached.(types.FetchConflictByTeamUUIDAndStatusAndSeverityResponse), nil
		}
	}
	fetch := func() (any, error) {
		if req.OrderBy == "" {
			models, err := queries.FetchConflictByTeamUUIDAndStatusAndSeverity(
				ctx,
				metichedb.FetchConflictByTeamUUIDAndStatusAndSeverityParams{
					TeamUUID: req.TeamUUID.String(),

					Status: req.Status.ToInt64(),

					Severity: req.Severity.ToInt64(),

					Offset: req.Offset,
					Limit:  req.Limit,
				},
			)

			if err != nil {

				return types.FetchConflictByTeamUUIDAndStatusAndSeverityResponse{}, err
			}
			return types.FetchConflictByTeamUUIDAndStatusAndSeverityResponse{
				Results: mapModelsToEntities(models),
			}, nil
		}

		err := errors.New("could not process request")

		return types.FetchConflictByTeamUUIDAndStatusAndSeverityResponse{}, err
	} // end fetch
	var v any
	var fetchErr error
	if skipShared {
		v, fetchErr = fetch()
	} else {
		v, fetchErr, _ = m.sg.Do(cacheKey, fetch)
	}
	if fetchErr != nil {
		return types.FetchConflictByTeamUUIDAndStatusAndSeverityResponse{}, fetchErr
	}
	result := v.(types.FetchConflictByTeamUUIDAndStatusAndSeverityResponse)
	if !skipShared {
		m.cache.Set(cacheKey, result, 0)
	}
	return result, nil

}
