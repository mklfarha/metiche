// Code generated by nuzur go-code-gen. DO NOT EDIT.

package invite

import (
	"context"
	"errors"

	"github.com/mklfarha/metiche/backend/core/module/invite/types"
	metichedb "github.com/mklfarha/metiche/backend/core/repository/gen"

	"time"

	"github.com/mklfarha/metiche/backend/entity/mapper"
)

func (m *module) Update(
	ctx context.Context,
	req types.UpsertRequest,
	opts ...Option,
) (types.UpsertResponse, error) {
	optConfig := applyAllOptions(opts)

	tx := optConfig.SQLTx
	createdTx := false
	if tx == nil {
		ntx, err := m.repository.DB.Begin()
		if err != nil {
			return types.UpsertResponse{}, err
		}
		tx = ntx
		defer tx.Rollback()
		createdTx = true
	}

	qtx := m.repository.Queries.WithTx(tx)
	existing, err := qtx.FetchInviteByIDForUpdate(ctx,
		req.Invite.ID.String(),
	)
	if err != nil {

		return types.UpsertResponse{}, err
	}

	if len(existing) == 0 {
		err := errors.New("entity not found")

		return types.UpsertResponse{}, err
	}

	// refresh server-managed timestamps that update on every write (updated_at)
	req.Invite.UpdatedAt = time.Now()

	// validate the merged entity against the schema's field type/type-config rules
	if err := req.Invite.Validate(); err != nil {

		return types.UpsertResponse{}, err
	}

	params := mapUpsertRequestToUpdateParams(req)
	err = qtx.UpdateInvite(
		ctx,
		params,
	)
	if err != nil {

		return types.UpsertResponse{}, err
	}

	if createdTx {
		err := tx.Commit()
		if err != nil {

			return types.UpsertResponse{}, err
		}
	}

	// The rows this module caches have changed, so everything it holds is
	// suspect: drop it rather than answer a later read from a snapshot that
	// predates this write. The cache is per entity, so this discards only this
	// entity's reads. (It is a no-op while a caller-supplied transaction is
	// still open — those reads bypass the cache anyway.)
	m.cache.Flush()

	return buildUpdateResponse(req), nil
}

func buildUpdateResponse(req types.UpsertRequest) types.UpsertResponse {
	return types.UpsertResponse{

		ID: req.Invite.ID,
	}
}

func mapUpsertRequestToUpdateParams(req types.UpsertRequest) metichedb.UpdateInviteParams {
	return metichedb.UpdateInviteParams{
		ID: req.Invite.ID.String(),

		TeamUUID: req.Invite.TeamUUID.String(),

		Code: req.Invite.Code,

		Label: req.Invite.Label,

		CreatedByMemberUUID: mapper.UUIDPtrToNullString(req.Invite.CreatedByMemberUUID),

		MaxUses: req.Invite.MaxUses,

		Uses: req.Invite.Uses,

		ExpiresAt: req.Invite.ExpiresAt,

		RevokedAt: req.Invite.RevokedAt,

		RevokedByMemberUUID: mapper.UUIDPtrToNullString(req.Invite.RevokedByMemberUUID),

		Status: req.Invite.Status.ToInt64(),

		LastUsedAt: req.Invite.LastUsedAt,

		CreatedAt: req.Invite.CreatedAt,

		UpdatedAt: req.Invite.UpdatedAt,
	}
}
