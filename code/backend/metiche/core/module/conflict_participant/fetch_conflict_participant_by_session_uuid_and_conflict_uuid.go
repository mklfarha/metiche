// Code generated by nuzur go-code-gen. DO NOT EDIT.

package conflict_participant

import (
	"context"
	"fmt"

	"github.com/mklfarha/metiche/backend/core/module/conflict_participant/types"

	"errors"
	metichedb "github.com/mklfarha/metiche/backend/core/repository/gen"
)

func (m *module) FetchConflictParticipantBySessionUUIDAndConflictUUID(
	ctx context.Context,
	req types.FetchConflictParticipantBySessionUUIDAndConflictUUIDRequest,
	opts ...Option,
) (types.FetchConflictParticipantBySessionUUIDAndConflictUUIDResponse, error) {

	resolvedOpts := applyAllOptions(opts)
	queries := m.repository.Queries
	if resolvedOpts.SQLTx != nil {
		queries = queries.WithTx(resolvedOpts.SQLTx)
	}
	// A read inside a transaction must observe that transaction's own
	// uncommitted writes, so it can neither be served from the shared cache nor
	// be collapsed into another caller's in-flight query.
	skipShared := resolvedOpts.SkipCache || resolvedOpts.SQLTx != nil
	cacheKey := fmt.Sprintf("FetchConflictParticipantBySessionUUIDAndConflictUUID:%v", req)
	if !skipShared {
		if cached, found := m.cache.Get(cacheKey); found {
			return cached.(types.FetchConflictParticipantBySessionUUIDAndConflictUUIDResponse), nil
		}
	}
	fetch := func() (any, error) {
		if req.OrderBy == "" {
			models, err := queries.FetchConflictParticipantBySessionUUIDAndConflictUUID(
				ctx,
				metichedb.FetchConflictParticipantBySessionUUIDAndConflictUUIDParams{
					SessionUUID: req.SessionUUID.String(),

					ConflictUUID: req.ConflictUUID.String(),

					Offset: req.Offset,
					Limit:  req.Limit,
				},
			)

			if err != nil {

				return types.FetchConflictParticipantBySessionUUIDAndConflictUUIDResponse{}, err
			}
			return types.FetchConflictParticipantBySessionUUIDAndConflictUUIDResponse{
				Results: mapModelsToEntities(models),
			}, nil
		}

		err := errors.New("could not process request")

		return types.FetchConflictParticipantBySessionUUIDAndConflictUUIDResponse{}, err
	} // end fetch
	var v any
	var fetchErr error
	if skipShared {
		v, fetchErr = fetch()
	} else {
		v, fetchErr, _ = m.sg.Do(cacheKey, fetch)
	}
	if fetchErr != nil {
		return types.FetchConflictParticipantBySessionUUIDAndConflictUUIDResponse{}, fetchErr
	}
	result := v.(types.FetchConflictParticipantBySessionUUIDAndConflictUUIDResponse)
	if !skipShared {
		m.cache.Set(cacheKey, result, 0)
	}
	return result, nil

}
