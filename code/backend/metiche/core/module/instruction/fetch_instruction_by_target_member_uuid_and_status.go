// Code generated by nuzur go-code-gen. DO NOT EDIT.

package instruction

import (
	"context"
	"fmt"

	"github.com/mklfarha/metiche/backend/core/module/instruction/types"

	"errors"
	metichedb "github.com/mklfarha/metiche/backend/core/repository/gen"

	"github.com/mklfarha/metiche/backend/entity/mapper"
)

func (m *module) FetchInstructionByTargetMemberUUIDAndStatus(
	ctx context.Context,
	req types.FetchInstructionByTargetMemberUUIDAndStatusRequest,
	opts ...Option,
) (types.FetchInstructionByTargetMemberUUIDAndStatusResponse, error) {

	resolvedOpts := applyAllOptions(opts)
	queries := m.repository.Queries
	if resolvedOpts.SQLTx != nil {
		queries = queries.WithTx(resolvedOpts.SQLTx)
	}
	// A read inside a transaction must observe that transaction's own
	// uncommitted writes, so it can neither be served from the shared cache nor
	// be collapsed into another caller's in-flight query.
	skipShared := resolvedOpts.SkipCache || resolvedOpts.SQLTx != nil
	cacheKey := fmt.Sprintf("FetchInstructionByTargetMemberUUIDAndStatus:%v", req)
	if !skipShared {
		if cached, found := m.cache.Get(cacheKey); found {
			return cached.(types.FetchInstructionByTargetMemberUUIDAndStatusResponse), nil
		}
	}
	fetch := func() (any, error) {
		if req.OrderBy == "" {
			models, err := queries.FetchInstructionByTargetMemberUUIDAndStatus(
				ctx,
				metichedb.FetchInstructionByTargetMemberUUIDAndStatusParams{
					TargetMemberUUID: mapper.UUIDPtrToNullString(req.TargetMemberUUID),

					Status: req.Status.ToInt64(),

					Offset: req.Offset,
					Limit:  req.Limit,
				},
			)

			if err != nil {

				return types.FetchInstructionByTargetMemberUUIDAndStatusResponse{}, err
			}
			return types.FetchInstructionByTargetMemberUUIDAndStatusResponse{
				Results: mapModelsToEntities(models),
			}, nil
		}

		err := errors.New("could not process request")

		return types.FetchInstructionByTargetMemberUUIDAndStatusResponse{}, err
	} // end fetch
	var v any
	var fetchErr error
	if skipShared {
		v, fetchErr = fetch()
	} else {
		v, fetchErr, _ = m.sg.Do(cacheKey, fetch)
	}
	if fetchErr != nil {
		return types.FetchInstructionByTargetMemberUUIDAndStatusResponse{}, fetchErr
	}
	result := v.(types.FetchInstructionByTargetMemberUUIDAndStatusResponse)
	if !skipShared {
		m.cache.Set(cacheKey, result, 0)
	}
	return result, nil

}
