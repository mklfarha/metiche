// Code generated by nuzur go-code-gen. DO NOT EDIT.

package board_login_link

import (
	"context"
	"fmt"

	"github.com/mklfarha/metiche/backend/core/module/board_login_link/types"

	"errors"
	metichedb "github.com/mklfarha/metiche/backend/core/repository/gen"
)

func (m *module) FetchBoardLoginLinkByAccountUUID(
	ctx context.Context,
	req types.FetchBoardLoginLinkByAccountUUIDRequest,
	opts ...Option,
) (types.FetchBoardLoginLinkByAccountUUIDResponse, error) {

	resolvedOpts := applyAllOptions(opts)
	queries := m.repository.Queries
	if resolvedOpts.SQLTx != nil {
		queries = queries.WithTx(resolvedOpts.SQLTx)
	}
	// A read inside a transaction must observe that transaction's own
	// uncommitted writes, so it can neither be served from the shared cache nor
	// be collapsed into another caller's in-flight query.
	skipShared := resolvedOpts.SkipCache || resolvedOpts.SQLTx != nil
	cacheKey := fmt.Sprintf("FetchBoardLoginLinkByAccountUUID:%v", req)
	if !skipShared {
		if cached, found := m.cache.Get(cacheKey); found {
			return cached.(types.FetchBoardLoginLinkByAccountUUIDResponse), nil
		}
	}
	fetch := func() (any, error) {
		if req.OrderBy == "" {
			models, err := queries.FetchBoardLoginLinkByAccountUUID(
				ctx,
				metichedb.FetchBoardLoginLinkByAccountUUIDParams{
					AccountUUID: req.AccountUUID.String(),

					Offset: req.Offset,
					Limit:  req.Limit,
				},
			)

			if err != nil {

				return types.FetchBoardLoginLinkByAccountUUIDResponse{}, err
			}
			return types.FetchBoardLoginLinkByAccountUUIDResponse{
				Results: mapModelsToEntities(models),
			}, nil
		}

		timeFields := []string{
			"expires_at",
		}

		orderByFieldFound := false
		for _, tf := range timeFields {
			if tf == req.OrderBy {
				orderByFieldFound = true
				break
			}
		}

		if !orderByFieldFound {
			err := errors.New("order by field not found")

			return types.FetchBoardLoginLinkByAccountUUIDResponse{}, err
		}

		sort := "ASC"
		if req.Sort != "" {
			if req.Sort == "ASC" || req.Sort == "DESC" {
				sort = req.Sort
			} else {
				err := errors.New("invalid sort value only ASC or DESC are valid")

				return types.FetchBoardLoginLinkByAccountUUIDResponse{}, err
			}
		}

		switch req.OrderBy {

		case "expires_at":
			if sort == "ASC" {
				models, err := queries.FetchBoardLoginLinkByAccountUUIDOrderedByExpiresAtASC(
					ctx,
					metichedb.FetchBoardLoginLinkByAccountUUIDOrderedByExpiresAtASCParams{
						AccountUUID: req.AccountUUID.String(),

						Offset: req.Offset,
						Limit:  req.Limit,
					},
				)
				if err != nil {

					return types.FetchBoardLoginLinkByAccountUUIDResponse{}, err
				}
				return types.FetchBoardLoginLinkByAccountUUIDResponse{
					Results: mapModelsToEntities(models),
				}, nil
			} else {
				models, err := queries.FetchBoardLoginLinkByAccountUUIDOrderedByExpiresAtDESC(
					ctx,
					metichedb.FetchBoardLoginLinkByAccountUUIDOrderedByExpiresAtDESCParams{
						AccountUUID: req.AccountUUID.String(),

						Offset: req.Offset,
						Limit:  req.Limit,
					},
				)
				if err != nil {

					return types.FetchBoardLoginLinkByAccountUUIDResponse{}, err
				}
				return types.FetchBoardLoginLinkByAccountUUIDResponse{
					Results: mapModelsToEntities(models),
				}, nil
			}

		}

		err := errors.New("could not process request")

		return types.FetchBoardLoginLinkByAccountUUIDResponse{}, err
	} // end fetch
	var v any
	var fetchErr error
	if skipShared {
		v, fetchErr = fetch()
	} else {
		v, fetchErr, _ = m.sg.Do(cacheKey, fetch)
	}
	if fetchErr != nil {
		return types.FetchBoardLoginLinkByAccountUUIDResponse{}, fetchErr
	}
	result := v.(types.FetchBoardLoginLinkByAccountUUIDResponse)
	if !skipShared {
		m.cache.Set(cacheKey, result, 0)
	}
	return result, nil

}
