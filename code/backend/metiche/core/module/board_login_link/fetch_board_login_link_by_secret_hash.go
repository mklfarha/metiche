// Code generated by nuzur go-code-gen. DO NOT EDIT.

package board_login_link

import (
	"context"
	"fmt"

	"github.com/mklfarha/metiche/backend/core/module/board_login_link/types"

	"errors"
	metichedb "github.com/mklfarha/metiche/backend/core/repository/gen"
)

func (m *module) FetchBoardLoginLinkBySecretHash(
	ctx context.Context,
	req types.FetchBoardLoginLinkBySecretHashRequest,
	opts ...Option,
) (types.FetchBoardLoginLinkBySecretHashResponse, error) {

	resolvedOpts := applyAllOptions(opts)
	queries := m.repository.Queries
	if resolvedOpts.SQLTx != nil {
		queries = queries.WithTx(resolvedOpts.SQLTx)
	}
	// A read inside a transaction must observe that transaction's own
	// uncommitted writes, so it can neither be served from the shared cache nor
	// be collapsed into another caller's in-flight query.
	skipShared := resolvedOpts.SkipCache || resolvedOpts.SQLTx != nil
	cacheKey := fmt.Sprintf("FetchBoardLoginLinkBySecretHash:%v", req)
	if !skipShared {
		if cached, found := m.cache.Get(cacheKey); found {
			return cached.(types.FetchBoardLoginLinkBySecretHashResponse), nil
		}
	}
	fetch := func() (any, error) {
		if req.OrderBy == "" {
			models, err := queries.FetchBoardLoginLinkBySecretHash(
				ctx,
				metichedb.FetchBoardLoginLinkBySecretHashParams{
					SecretHash: req.SecretHash,

					Offset: req.Offset,
					Limit:  req.Limit,
				},
			)

			if err != nil {

				return types.FetchBoardLoginLinkBySecretHashResponse{}, err
			}
			return types.FetchBoardLoginLinkBySecretHashResponse{
				Results: mapModelsToEntities(models),
			}, nil
		}

		timeFields := []string{
			"expires_at",
		}

		orderByFieldFound := false
		for _, tf := range timeFields {
			if tf == req.OrderBy {
				orderByFieldFound = true
				break
			}
		}

		if !orderByFieldFound {
			err := errors.New("order by field not found")

			return types.FetchBoardLoginLinkBySecretHashResponse{}, err
		}

		sort := "ASC"
		if req.Sort != "" {
			if req.Sort == "ASC" || req.Sort == "DESC" {
				sort = req.Sort
			} else {
				err := errors.New("invalid sort value only ASC or DESC are valid")

				return types.FetchBoardLoginLinkBySecretHashResponse{}, err
			}
		}

		switch req.OrderBy {

		case "expires_at":
			if sort == "ASC" {
				models, err := queries.FetchBoardLoginLinkBySecretHashOrderedByExpiresAtASC(
					ctx,
					metichedb.FetchBoardLoginLinkBySecretHashOrderedByExpiresAtASCParams{
						SecretHash: req.SecretHash,

						Offset: req.Offset,
						Limit:  req.Limit,
					},
				)
				if err != nil {

					return types.FetchBoardLoginLinkBySecretHashResponse{}, err
				}
				return types.FetchBoardLoginLinkBySecretHashResponse{
					Results: mapModelsToEntities(models),
				}, nil
			} else {
				models, err := queries.FetchBoardLoginLinkBySecretHashOrderedByExpiresAtDESC(
					ctx,
					metichedb.FetchBoardLoginLinkBySecretHashOrderedByExpiresAtDESCParams{
						SecretHash: req.SecretHash,

						Offset: req.Offset,
						Limit:  req.Limit,
					},
				)
				if err != nil {

					return types.FetchBoardLoginLinkBySecretHashResponse{}, err
				}
				return types.FetchBoardLoginLinkBySecretHashResponse{
					Results: mapModelsToEntities(models),
				}, nil
			}

		}

		err := errors.New("could not process request")

		return types.FetchBoardLoginLinkBySecretHashResponse{}, err
	} // end fetch
	var v any
	var fetchErr error
	if skipShared {
		v, fetchErr = fetch()
	} else {
		v, fetchErr, _ = m.sg.Do(cacheKey, fetch)
	}
	if fetchErr != nil {
		return types.FetchBoardLoginLinkBySecretHashResponse{}, fetchErr
	}
	result := v.(types.FetchBoardLoginLinkBySecretHashResponse)
	if !skipShared {
		m.cache.Set(cacheKey, result, 0)
	}
	return result, nil

}
