// Code generated by nuzur go-code-gen. DO NOT EDIT.

package judgement

import (
	"context"
	"database/sql"
	"github.com/mklfarha/metiche/backend/core/module/judgement/types"
	repogen "github.com/mklfarha/metiche/backend/core/repository/gen"
	"github.com/mklfarha/metiche/backend/core/repository/list"
	main_entity "github.com/mklfarha/metiche/backend/entity/judgement"

	"go.uber.org/zap"
	"slices"
)

func (m *module) List(ctx context.Context,
	request types.ListRequest,
	opts ...Option) (types.ListResponse, error) {

	reqPlusOne := request
	reqPlusOne.PageSize = request.PageSize + 1

	query, args, err := m.repository.BuildListEntityQuery(
		ctx,
		reqPlusOne,
		main_entity.Judgement{},
		false)
	if err != nil {

		return types.ListResponse{}, err
	}

	resolvedOpts := applyAllOptions(opts)
	// A read inside a transaction must observe that transaction's own
	// uncommitted writes, so it runs on the transaction and can neither be
	// served from the shared cache nor be collapsed into another caller's
	// in-flight query.
	var querier interface {
		QueryContext(ctx context.Context, query string, args ...any) (*sql.Rows, error)
	} = m.repository.DB
	if resolvedOpts.SQLTx != nil {
		querier = resolvedOpts.SQLTx
	}
	skipShared := resolvedOpts.SkipCache || resolvedOpts.SQLTx != nil
	// Keyed on the BUILT QUERY *and its arguments*, which together encode the
	// filter, the ordering, the projection and the page. Formatting the request
	// instead put the *filtering.Declarations POINTER in the key — a fresh
	// allocation on every request — so no two keys could ever match: the cache
	// only grew, and the singleflight group never collapsed anything.
	//
	// The args are in the key because the filter VALUES are no longer in the
	// query text: two callers filtering on different values now build the SAME
	// text, so keying on the text alone would serve one of them the other's rows
	// — and the singleflight group shares this key, so it would not even take a
	// cache hit to do it.
	cacheKey := list.CacheKey("ListJudgement", query, args)
	if !skipShared {
		if cached, found := m.cache.Get(cacheKey); found {
			return cached.(types.ListResponse), nil
		}
	}

	// Named runList, not list: the `list` identifier now refers to the repository
	// list package (CacheKey above), and a local of the same name would shadow it
	// for anything added below.
	runList := func() (any, error) {
		var rows *sql.Rows
		// The args MUST be passed: the built query carries placeholders where the
		// filter values used to be interpolated, so calling QueryContext without
		// them is a bind error, and interpolating them back into the text is the
		// injection this replaced.
		rows, err = querier.QueryContext(ctx, query, args...)
		if err != nil {

			// The query text is logged; the ARGS are not, only their count. The
			// values are caller data, and they are exactly what used to end up in
			// the text — logging them would put them back in every log sink.
			m.logger.Error("error in executing query for ListJudgement", zap.String("query", query), zap.Int("args", len(args)), zap.Error(err))
			return types.ListResponse{}, err
		}
		defer rows.Close()
		var scanGetters []func(*repogen.Judgement) any
		if len(request.GetIncludeFields()) > 0 {
			for _, f := range listFields {
				if slices.Contains(request.GetIncludeFields(), f) {
					scanGetters = append(scanGetters, listFieldRegistry[f])
				}
			}
		} else if len(request.GetExcludeFields()) > 0 {
			for _, f := range listFields {
				if !slices.Contains(request.GetExcludeFields(), f) {
					scanGetters = append(scanGetters, listFieldRegistry[f])
				}
			}
		} else {
			for _, f := range listFields {
				scanGetters = append(scanGetters, listFieldRegistry[f])
			}
		}

		var items []repogen.Judgement
		for rows.Next() {
			var i repogen.Judgement
			fields := make([]any, 0, len(scanGetters))
			for _, getter := range scanGetters {
				fields = append(fields, getter(&i))
			}
			if err := rows.Scan(fields...); err != nil {

				return types.ListResponse{}, err
			}
			items = append(items, i)
		}
		if err := rows.Close(); err != nil {

			return types.ListResponse{}, err
		}
		if err := rows.Err(); err != nil {

			return types.ListResponse{}, err
		}

		hasNextPage := false
		if len(items) > int(request.PageSize) {
			hasNextPage = true
			items = items[:request.PageSize]
		}

		return types.ListResponse{
			Judgement:   mapModelsToEntities(items),
			HasNextPage: hasNextPage,
		}, nil
	}
	var v any
	var listErr error
	if skipShared {
		v, listErr = runList()
	} else {
		v, listErr, _ = m.sg.Do(cacheKey, runList)
	}
	if listErr != nil {
		return types.ListResponse{}, listErr
	}
	listResponse := v.(types.ListResponse)
	if !skipShared {
		m.cache.Set(cacheKey, listResponse, 0)
	}
	return listResponse, nil
}

var listFields = []string{

	"id",

	"team_uuid",

	"pair_key",

	"kind",

	"subject_a_kind",

	"subject_a_uuid",

	"subject_a_revision",

	"subject_b_kind",

	"subject_b_uuid",

	"subject_b_revision",

	"status",

	"verdict",

	"severity",

	"confidence",

	"rationale",

	"judge_session_uuid",

	"judging_expires_at",

	"conflict_uuid",

	"pinned",

	"created_at",

	"updated_at",
}

var listFieldRegistry = map[string]func(*repogen.Judgement) any{

	"id": func(i *repogen.Judgement) any { return &i.ID },

	"team_uuid": func(i *repogen.Judgement) any { return &i.TeamUUID },

	"pair_key": func(i *repogen.Judgement) any { return &i.PairKey },

	"kind": func(i *repogen.Judgement) any { return &i.Kind },

	"subject_a_kind": func(i *repogen.Judgement) any { return &i.SubjectAKind },

	"subject_a_uuid": func(i *repogen.Judgement) any { return &i.SubjectAUUID },

	"subject_a_revision": func(i *repogen.Judgement) any { return &i.SubjectARevision },

	"subject_b_kind": func(i *repogen.Judgement) any { return &i.SubjectBKind },

	"subject_b_uuid": func(i *repogen.Judgement) any { return &i.SubjectBUUID },

	"subject_b_revision": func(i *repogen.Judgement) any { return &i.SubjectBRevision },

	"status": func(i *repogen.Judgement) any { return &i.Status },

	"verdict": func(i *repogen.Judgement) any { return &i.Verdict },

	"severity": func(i *repogen.Judgement) any { return &i.Severity },

	"confidence": func(i *repogen.Judgement) any { return &i.Confidence },

	"rationale": func(i *repogen.Judgement) any { return &i.Rationale },

	"judge_session_uuid": func(i *repogen.Judgement) any { return &i.JudgeSessionUUID },

	"judging_expires_at": func(i *repogen.Judgement) any { return &i.JudgingExpiresAt },

	"conflict_uuid": func(i *repogen.Judgement) any { return &i.ConflictUUID },

	"pinned": func(i *repogen.Judgement) any { return &i.Pinned },

	"created_at": func(i *repogen.Judgement) any { return &i.CreatedAt },

	"updated_at": func(i *repogen.Judgement) any { return &i.UpdatedAt },
}
