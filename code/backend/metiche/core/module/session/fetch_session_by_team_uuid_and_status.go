// Code generated by nuzur go-code-gen. DO NOT EDIT.

package session

import (
	"context"
	"fmt"

	"github.com/mklfarha/metiche/backend/core/module/session/types"

	"errors"
	metichedb "github.com/mklfarha/metiche/backend/core/repository/gen"
)

func (m *module) FetchSessionByTeamUUIDAndStatus(
	ctx context.Context,
	req types.FetchSessionByTeamUUIDAndStatusRequest,
	opts ...Option,
) (types.FetchSessionByTeamUUIDAndStatusResponse, error) {

	resolvedOpts := applyAllOptions(opts)
	queries := m.repository.Queries
	if resolvedOpts.SQLTx != nil {
		queries = queries.WithTx(resolvedOpts.SQLTx)
	}
	// A read inside a transaction must observe that transaction's own
	// uncommitted writes, so it can neither be served from the shared cache nor
	// be collapsed into another caller's in-flight query.
	skipShared := resolvedOpts.SkipCache || resolvedOpts.SQLTx != nil
	cacheKey := fmt.Sprintf("FetchSessionByTeamUUIDAndStatus:%v", req)
	if !skipShared {
		if cached, found := m.cache.Get(cacheKey); found {
			return cached.(types.FetchSessionByTeamUUIDAndStatusResponse), nil
		}
	}
	fetch := func() (any, error) {
		if req.OrderBy == "" {
			models, err := queries.FetchSessionByTeamUUIDAndStatus(
				ctx,
				metichedb.FetchSessionByTeamUUIDAndStatusParams{
					TeamUUID: req.TeamUUID.String(),

					Status: req.Status.ToInt64(),

					Offset: req.Offset,
					Limit:  req.Limit,
				},
			)

			if err != nil {

				return types.FetchSessionByTeamUUIDAndStatusResponse{}, err
			}
			return types.FetchSessionByTeamUUIDAndStatusResponse{
				Results: mapModelsToEntities(models),
			}, nil
		}

		err := errors.New("could not process request")

		return types.FetchSessionByTeamUUIDAndStatusResponse{}, err
	} // end fetch
	var v any
	var fetchErr error
	if skipShared {
		v, fetchErr = fetch()
	} else {
		v, fetchErr, _ = m.sg.Do(cacheKey, fetch)
	}
	if fetchErr != nil {
		return types.FetchSessionByTeamUUIDAndStatusResponse{}, fetchErr
	}
	result := v.(types.FetchSessionByTeamUUIDAndStatusResponse)
	if !skipShared {
		m.cache.Set(cacheKey, result, 0)
	}
	return result, nil

}
