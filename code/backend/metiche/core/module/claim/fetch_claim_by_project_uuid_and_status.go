// Code generated by nuzur go-code-gen. DO NOT EDIT.

package claim

import (
	"context"
	"fmt"

	"github.com/mklfarha/metiche/backend/core/module/claim/types"

	"errors"
	metichedb "github.com/mklfarha/metiche/backend/core/repository/gen"
)

func (m *module) FetchClaimByProjectUUIDAndStatus(
	ctx context.Context,
	req types.FetchClaimByProjectUUIDAndStatusRequest,
	opts ...Option,
) (types.FetchClaimByProjectUUIDAndStatusResponse, error) {

	resolvedOpts := applyAllOptions(opts)
	queries := m.repository.Queries
	if resolvedOpts.SQLTx != nil {
		queries = queries.WithTx(resolvedOpts.SQLTx)
	}
	// A read inside a transaction must observe that transaction's own
	// uncommitted writes, so it can neither be served from the shared cache nor
	// be collapsed into another caller's in-flight query.
	skipShared := resolvedOpts.SkipCache || resolvedOpts.SQLTx != nil
	cacheKey := fmt.Sprintf("FetchClaimByProjectUUIDAndStatus:%v", req)
	if !skipShared {
		if cached, found := m.cache.Get(cacheKey); found {
			return cached.(types.FetchClaimByProjectUUIDAndStatusResponse), nil
		}
	}
	fetch := func() (any, error) {
		if req.OrderBy == "" {
			models, err := queries.FetchClaimByProjectUUIDAndStatus(
				ctx,
				metichedb.FetchClaimByProjectUUIDAndStatusParams{
					ProjectUUID: req.ProjectUUID.String(),

					Status: req.Status.ToInt64(),

					Offset: req.Offset,
					Limit:  req.Limit,
				},
			)

			if err != nil {

				return types.FetchClaimByProjectUUIDAndStatusResponse{}, err
			}
			return types.FetchClaimByProjectUUIDAndStatusResponse{
				Results: mapModelsToEntities(models),
			}, nil
		}

		err := errors.New("could not process request")

		return types.FetchClaimByProjectUUIDAndStatusResponse{}, err
	} // end fetch
	var v any
	var fetchErr error
	if skipShared {
		v, fetchErr = fetch()
	} else {
		v, fetchErr, _ = m.sg.Do(cacheKey, fetch)
	}
	if fetchErr != nil {
		return types.FetchClaimByProjectUUIDAndStatusResponse{}, fetchErr
	}
	result := v.(types.FetchClaimByProjectUUIDAndStatusResponse)
	if !skipShared {
		m.cache.Set(cacheKey, result, 0)
	}
	return result, nil

}
