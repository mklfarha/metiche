// Code generated by nuzur go-code-gen. DO NOT EDIT.

package agent

import (
	"context"
	"fmt"

	"github.com/mklfarha/metiche/backend/core/module/agent/types"

	"errors"
	metichedb "github.com/mklfarha/metiche/backend/core/repository/gen"
)

func (m *module) FetchAgentByMemberUUIDAndClientKey(
	ctx context.Context,
	req types.FetchAgentByMemberUUIDAndClientKeyRequest,
	opts ...Option,
) (types.FetchAgentByMemberUUIDAndClientKeyResponse, error) {

	resolvedOpts := applyAllOptions(opts)
	queries := m.repository.Queries
	if resolvedOpts.SQLTx != nil {
		queries = queries.WithTx(resolvedOpts.SQLTx)
	}
	// A read inside a transaction must observe that transaction's own
	// uncommitted writes, so it can neither be served from the shared cache nor
	// be collapsed into another caller's in-flight query.
	skipShared := resolvedOpts.SkipCache || resolvedOpts.SQLTx != nil
	cacheKey := fmt.Sprintf("FetchAgentByMemberUUIDAndClientKey:%v", req)
	if !skipShared {
		if cached, found := m.cache.Get(cacheKey); found {
			return cached.(types.FetchAgentByMemberUUIDAndClientKeyResponse), nil
		}
	}
	fetch := func() (any, error) {
		if req.OrderBy == "" {
			models, err := queries.FetchAgentByMemberUUIDAndClientKey(
				ctx,
				metichedb.FetchAgentByMemberUUIDAndClientKeyParams{
					MemberUUID: req.MemberUUID.String(),

					ClientKey: req.ClientKey,

					Offset: req.Offset,
					Limit:  req.Limit,
				},
			)

			if err != nil {

				return types.FetchAgentByMemberUUIDAndClientKeyResponse{}, err
			}
			return types.FetchAgentByMemberUUIDAndClientKeyResponse{
				Results: mapModelsToEntities(models),
			}, nil
		}

		err := errors.New("could not process request")

		return types.FetchAgentByMemberUUIDAndClientKeyResponse{}, err
	} // end fetch
	var v any
	var fetchErr error
	if skipShared {
		v, fetchErr = fetch()
	} else {
		v, fetchErr, _ = m.sg.Do(cacheKey, fetch)
	}
	if fetchErr != nil {
		return types.FetchAgentByMemberUUIDAndClientKeyResponse{}, fetchErr
	}
	result := v.(types.FetchAgentByMemberUUIDAndClientKeyResponse)
	if !skipShared {
		m.cache.Set(cacheKey, result, 0)
	}
	return result, nil

}
