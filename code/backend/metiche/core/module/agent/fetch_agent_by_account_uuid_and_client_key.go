// Code generated by nuzur go-code-gen. DO NOT EDIT.

package agent

import (
	"context"
	"fmt"

	"github.com/mklfarha/metiche/backend/core/module/agent/types"

	"errors"
	metichedb "github.com/mklfarha/metiche/backend/core/repository/gen"
)

func (m *module) FetchAgentByAccountUUIDAndClientKey(
	ctx context.Context,
	req types.FetchAgentByAccountUUIDAndClientKeyRequest,
	opts ...Option,
) (types.FetchAgentByAccountUUIDAndClientKeyResponse, error) {

	resolvedOpts := applyAllOptions(opts)
	queries := m.repository.Queries
	if resolvedOpts.SQLTx != nil {
		queries = queries.WithTx(resolvedOpts.SQLTx)
	}
	// A read inside a transaction must observe that transaction's own
	// uncommitted writes, so it can neither be served from the shared cache nor
	// be collapsed into another caller's in-flight query.
	skipShared := resolvedOpts.SkipCache || resolvedOpts.SQLTx != nil
	cacheKey := fmt.Sprintf("FetchAgentByAccountUUIDAndClientKey:%v", req)
	if !skipShared {
		if cached, found := m.cache.Get(cacheKey); found {
			return cached.(types.FetchAgentByAccountUUIDAndClientKeyResponse), nil
		}
	}
	fetch := func() (any, error) {
		if req.OrderBy == "" {
			models, err := queries.FetchAgentByAccountUUIDAndClientKey(
				ctx,
				metichedb.FetchAgentByAccountUUIDAndClientKeyParams{
					AccountUUID: req.AccountUUID.String(),

					ClientKey: req.ClientKey,

					Offset: req.Offset,
					Limit:  req.Limit,
				},
			)

			if err != nil {

				return types.FetchAgentByAccountUUIDAndClientKeyResponse{}, err
			}
			return types.FetchAgentByAccountUUIDAndClientKeyResponse{
				Results: mapModelsToEntities(models),
			}, nil
		}

		err := errors.New("could not process request")

		return types.FetchAgentByAccountUUIDAndClientKeyResponse{}, err
	} // end fetch
	var v any
	var fetchErr error
	if skipShared {
		v, fetchErr = fetch()
	} else {
		v, fetchErr, _ = m.sg.Do(cacheKey, fetch)
	}
	if fetchErr != nil {
		return types.FetchAgentByAccountUUIDAndClientKeyResponse{}, fetchErr
	}
	result := v.(types.FetchAgentByAccountUUIDAndClientKeyResponse)
	if !skipShared {
		m.cache.Set(cacheKey, result, 0)
	}
	return result, nil

}
