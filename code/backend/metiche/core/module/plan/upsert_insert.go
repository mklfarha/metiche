// Code generated by nuzur go-code-gen. DO NOT EDIT.

package plan

import (
	"context"

	"github.com/mklfarha/metiche/backend/core/module/plan/types"
	metichedb "github.com/mklfarha/metiche/backend/core/repository/gen"

	"github.com/gofrs/uuid"

	"time"
)

func (m *module) Insert(
	ctx context.Context,
	req types.UpsertRequest,
	opts ...Option,
) (types.UpsertResponse, error) {

	// auto-generate primary-key UUIDs for new records when not supplied by the caller
	if req.Plan.ID == uuid.Nil {
		req.Plan.ID = uuid.Must(uuid.NewV4())
	}

	// server-managed timestamps (generated created_at/updated_at)
	req.Plan.CreatedAt = time.Now()
	req.Plan.UpdatedAt = time.Now()

	// validate the new record against the schema's field type/type-config rules
	if err := req.Plan.Validate(); err != nil {

		return types.UpsertResponse{}, err
	}

	optConfig := applyAllOptions(opts)

	tx := optConfig.SQLTx
	createdTx := false
	if tx == nil {
		ntx, err := m.repository.DB.Begin()
		if err != nil {
			return types.UpsertResponse{}, err
		}
		tx = ntx
		defer tx.Rollback()
		createdTx = true
	}

	qtx := m.repository.Queries.WithTx(tx)
	params := mapUpsertRequestToInsertParams(req)

	_, err := qtx.InsertPlan(
		ctx,
		params,
	)
	if err != nil {

		return types.UpsertResponse{}, err
	}

	if createdTx {
		err := tx.Commit()
		if err != nil {

			return types.UpsertResponse{}, err
		}
	}

	// The rows this module caches have changed, so everything it holds is
	// suspect: drop it rather than answer a later read from a snapshot that
	// predates this write. The cache is per entity, so this discards only this
	// entity's reads. (It is a no-op while a caller-supplied transaction is
	// still open — those reads bypass the cache anyway.)
	m.cache.Flush()

	return buildInsertResponse(req), nil
}

func buildInsertResponse(req types.UpsertRequest) types.UpsertResponse {
	return types.UpsertResponse{

		ID: req.Plan.ID,
	}
}

func mapUpsertRequestToInsertParams(req types.UpsertRequest) metichedb.InsertPlanParams {
	return metichedb.InsertPlanParams{
		ID: req.Plan.ID.String(),

		Key: req.Plan.Key,

		Name: req.Plan.Name,

		Description: req.Plan.Description,

		MaxConcurrentAgents: req.Plan.MaxConcurrentAgents,

		RetentionDays: req.Plan.RetentionDays,

		MaxMembers: req.Plan.MaxMembers,

		MaxProjects: req.Plan.MaxProjects,

		IsInstanceDefault: req.Plan.IsInstanceDefault,

		SortOrder: req.Plan.SortOrder,

		Status: req.Plan.Status.ToInt64(),

		CreatedAt: req.Plan.CreatedAt,

		UpdatedAt: req.Plan.UpdatedAt,
	}
}
