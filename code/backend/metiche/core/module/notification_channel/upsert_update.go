// Code generated by nuzur go-code-gen. DO NOT EDIT.

package notification_channel

import (
	"context"
	"errors"

	"github.com/mklfarha/metiche/backend/core/module/notification_channel/types"
	metichedb "github.com/mklfarha/metiche/backend/core/repository/gen"

	"time"

	"github.com/mklfarha/metiche/backend/entity/mapper"
)

func (m *module) Update(
	ctx context.Context,
	req types.UpsertRequest,
	opts ...Option,
) (types.UpsertResponse, error) {
	optConfig := applyAllOptions(opts)

	tx := optConfig.SQLTx
	createdTx := false
	if tx == nil {
		ntx, err := m.repository.DB.Begin()
		if err != nil {
			return types.UpsertResponse{}, err
		}
		tx = ntx
		defer tx.Rollback()
		createdTx = true
	}

	qtx := m.repository.Queries.WithTx(tx)
	existing, err := qtx.FetchNotificationChannelByIDForUpdate(ctx,
		req.NotificationChannel.ID.String(),
	)
	if err != nil {

		return types.UpsertResponse{}, err
	}

	if len(existing) == 0 {
		err := errors.New("entity not found")

		return types.UpsertResponse{}, err
	}

	// refresh server-managed timestamps that update on every write (updated_at)
	req.NotificationChannel.UpdatedAt = time.Now()

	// validate the merged entity against the schema's field type/type-config rules
	if err := req.NotificationChannel.Validate(); err != nil {

		return types.UpsertResponse{}, err
	}

	params := mapUpsertRequestToUpdateParams(req)
	err = qtx.UpdateNotificationChannel(
		ctx,
		params,
	)
	if err != nil {

		return types.UpsertResponse{}, err
	}

	if createdTx {
		err := tx.Commit()
		if err != nil {

			return types.UpsertResponse{}, err
		}
	}

	// The rows this module caches have changed, so everything it holds is
	// suspect: drop it rather than answer a later read from a snapshot that
	// predates this write. The cache is per entity, so this discards only this
	// entity's reads. (It is a no-op while a caller-supplied transaction is
	// still open — those reads bypass the cache anyway.)
	m.cache.Flush()

	return buildUpdateResponse(req), nil
}

func buildUpdateResponse(req types.UpsertRequest) types.UpsertResponse {
	return types.UpsertResponse{

		ID: req.NotificationChannel.ID,
	}
}

func mapUpsertRequestToUpdateParams(req types.UpsertRequest) metichedb.UpdateNotificationChannelParams {
	return metichedb.UpdateNotificationChannelParams{
		ID: req.NotificationChannel.ID.String(),

		TeamUUID: req.NotificationChannel.TeamUUID.String(),

		ProjectUUID: mapper.UUIDPtrToNullString(req.NotificationChannel.ProjectUUID),

		Key: req.NotificationChannel.Key,

		Kind: req.NotificationChannel.Kind.ToInt64(),

		Label: req.NotificationChannel.Label,

		TargetURL: req.NotificationChannel.TargetURL,

		MinSeverity: req.NotificationChannel.MinSeverity.ToInt64(),

		NotifyHumanRequests: req.NotificationChannel.NotifyHumanRequests,

		Status: req.NotificationChannel.Status.ToInt64(),

		DeliveryStatus: req.NotificationChannel.DeliveryStatus.ToInt64(),

		ConsecutiveFailures: req.NotificationChannel.ConsecutiveFailures,

		LastAttemptAt: req.NotificationChannel.LastAttemptAt,

		LastSuccessAt: req.NotificationChannel.LastSuccessAt,

		LastError: req.NotificationChannel.LastError,

		CreatedByMemberUUID: mapper.UUIDPtrToNullString(req.NotificationChannel.CreatedByMemberUUID),

		CreatedAt: req.NotificationChannel.CreatedAt,

		UpdatedAt: req.NotificationChannel.UpdatedAt,
	}
}
