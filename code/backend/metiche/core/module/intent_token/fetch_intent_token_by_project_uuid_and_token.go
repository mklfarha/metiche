// Code generated by nuzur go-code-gen. DO NOT EDIT.

package intent_token

import (
	"context"
	"fmt"

	"github.com/mklfarha/metiche/backend/core/module/intent_token/types"

	"errors"
	metichedb "github.com/mklfarha/metiche/backend/core/repository/gen"
)

func (m *module) FetchIntentTokenByProjectUUIDAndToken(
	ctx context.Context,
	req types.FetchIntentTokenByProjectUUIDAndTokenRequest,
	opts ...Option,
) (types.FetchIntentTokenByProjectUUIDAndTokenResponse, error) {

	resolvedOpts := applyAllOptions(opts)
	queries := m.repository.Queries
	if resolvedOpts.SQLTx != nil {
		queries = queries.WithTx(resolvedOpts.SQLTx)
	}
	// A read inside a transaction must observe that transaction's own
	// uncommitted writes, so it can neither be served from the shared cache nor
	// be collapsed into another caller's in-flight query.
	skipShared := resolvedOpts.SkipCache || resolvedOpts.SQLTx != nil
	cacheKey := fmt.Sprintf("FetchIntentTokenByProjectUUIDAndToken:%v", req)
	if !skipShared {
		if cached, found := m.cache.Get(cacheKey); found {
			return cached.(types.FetchIntentTokenByProjectUUIDAndTokenResponse), nil
		}
	}
	fetch := func() (any, error) {
		if req.OrderBy == "" {
			models, err := queries.FetchIntentTokenByProjectUUIDAndToken(
				ctx,
				metichedb.FetchIntentTokenByProjectUUIDAndTokenParams{
					ProjectUUID: req.ProjectUUID.String(),

					Token: req.Token,

					Offset: req.Offset,
					Limit:  req.Limit,
				},
			)

			if err != nil {

				return types.FetchIntentTokenByProjectUUIDAndTokenResponse{}, err
			}
			return types.FetchIntentTokenByProjectUUIDAndTokenResponse{
				Results: mapModelsToEntities(models),
			}, nil
		}

		err := errors.New("could not process request")

		return types.FetchIntentTokenByProjectUUIDAndTokenResponse{}, err
	} // end fetch
	var v any
	var fetchErr error
	if skipShared {
		v, fetchErr = fetch()
	} else {
		v, fetchErr, _ = m.sg.Do(cacheKey, fetch)
	}
	if fetchErr != nil {
		return types.FetchIntentTokenByProjectUUIDAndTokenResponse{}, fetchErr
	}
	result := v.(types.FetchIntentTokenByProjectUUIDAndTokenResponse)
	if !skipShared {
		m.cache.Set(cacheKey, result, 0)
	}
	return result, nil

}
