// Code generated by nuzur go-code-gen. DO NOT EDIT.

package intent

import (
	"context"
	"fmt"

	"github.com/mklfarha/metiche/backend/core/module/intent/types"

	"errors"
	metichedb "github.com/mklfarha/metiche/backend/core/repository/gen"
)

func (m *module) FetchIntentByProjectUUIDAndStatus(
	ctx context.Context,
	req types.FetchIntentByProjectUUIDAndStatusRequest,
	opts ...Option,
) (types.FetchIntentByProjectUUIDAndStatusResponse, error) {

	resolvedOpts := applyAllOptions(opts)
	queries := m.repository.Queries
	if resolvedOpts.SQLTx != nil {
		queries = queries.WithTx(resolvedOpts.SQLTx)
	}
	// A read inside a transaction must observe that transaction's own
	// uncommitted writes, so it can neither be served from the shared cache nor
	// be collapsed into another caller's in-flight query.
	skipShared := resolvedOpts.SkipCache || resolvedOpts.SQLTx != nil
	cacheKey := fmt.Sprintf("FetchIntentByProjectUUIDAndStatus:%v", req)
	if !skipShared {
		if cached, found := m.cache.Get(cacheKey); found {
			return cached.(types.FetchIntentByProjectUUIDAndStatusResponse), nil
		}
	}
	fetch := func() (any, error) {
		if req.OrderBy == "" {
			models, err := queries.FetchIntentByProjectUUIDAndStatus(
				ctx,
				metichedb.FetchIntentByProjectUUIDAndStatusParams{
					ProjectUUID: req.ProjectUUID.String(),

					Status: req.Status.ToInt64(),

					Offset: req.Offset,
					Limit:  req.Limit,
				},
			)

			if err != nil {

				return types.FetchIntentByProjectUUIDAndStatusResponse{}, err
			}
			return types.FetchIntentByProjectUUIDAndStatusResponse{
				Results: mapModelsToEntities(models),
			}, nil
		}

		err := errors.New("could not process request")

		return types.FetchIntentByProjectUUIDAndStatusResponse{}, err
	} // end fetch
	var v any
	var fetchErr error
	if skipShared {
		v, fetchErr = fetch()
	} else {
		v, fetchErr, _ = m.sg.Do(cacheKey, fetch)
	}
	if fetchErr != nil {
		return types.FetchIntentByProjectUUIDAndStatusResponse{}, fetchErr
	}
	result := v.(types.FetchIntentByProjectUUIDAndStatusResponse)
	if !skipShared {
		m.cache.Set(cacheKey, result, 0)
	}
	return result, nil

}
