// Code generated by nuzur go-code-gen. DO NOT EDIT.

package decision_token

import (
	"context"
	"fmt"

	"github.com/mklfarha/metiche/backend/core/module/decision_token/types"

	"errors"
	metichedb "github.com/mklfarha/metiche/backend/core/repository/gen"
)

func (m *module) FetchDecisionTokenByTeamUUIDAndToken(
	ctx context.Context,
	req types.FetchDecisionTokenByTeamUUIDAndTokenRequest,
	opts ...Option,
) (types.FetchDecisionTokenByTeamUUIDAndTokenResponse, error) {

	resolvedOpts := applyAllOptions(opts)
	queries := m.repository.Queries
	if resolvedOpts.SQLTx != nil {
		queries = queries.WithTx(resolvedOpts.SQLTx)
	}
	// A read inside a transaction must observe that transaction's own
	// uncommitted writes, so it can neither be served from the shared cache nor
	// be collapsed into another caller's in-flight query.
	skipShared := resolvedOpts.SkipCache || resolvedOpts.SQLTx != nil
	cacheKey := fmt.Sprintf("FetchDecisionTokenByTeamUUIDAndToken:%v", req)
	if !skipShared {
		if cached, found := m.cache.Get(cacheKey); found {
			return cached.(types.FetchDecisionTokenByTeamUUIDAndTokenResponse), nil
		}
	}
	fetch := func() (any, error) {
		if req.OrderBy == "" {
			models, err := queries.FetchDecisionTokenByTeamUUIDAndToken(
				ctx,
				metichedb.FetchDecisionTokenByTeamUUIDAndTokenParams{
					TeamUUID: req.TeamUUID.String(),

					Token: req.Token,

					Offset: req.Offset,
					Limit:  req.Limit,
				},
			)

			if err != nil {

				return types.FetchDecisionTokenByTeamUUIDAndTokenResponse{}, err
			}
			return types.FetchDecisionTokenByTeamUUIDAndTokenResponse{
				Results: mapModelsToEntities(models),
			}, nil
		}

		err := errors.New("could not process request")

		return types.FetchDecisionTokenByTeamUUIDAndTokenResponse{}, err
	} // end fetch
	var v any
	var fetchErr error
	if skipShared {
		v, fetchErr = fetch()
	} else {
		v, fetchErr, _ = m.sg.Do(cacheKey, fetch)
	}
	if fetchErr != nil {
		return types.FetchDecisionTokenByTeamUUIDAndTokenResponse{}, fetchErr
	}
	result := v.(types.FetchDecisionTokenByTeamUUIDAndTokenResponse)
	if !skipShared {
		m.cache.Set(cacheKey, result, 0)
	}
	return result, nil

}
