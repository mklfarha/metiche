// Code generated by nuzur go-code-gen. DO NOT EDIT.

package team_event

import (
	"context"
	"fmt"

	"github.com/mklfarha/metiche/backend/core/module/team_event/types"

	"errors"
	metichedb "github.com/mklfarha/metiche/backend/core/repository/gen"
)

func (m *module) FetchTeamEventByTeamUUIDAndSequence(
	ctx context.Context,
	req types.FetchTeamEventByTeamUUIDAndSequenceRequest,
	opts ...Option,
) (types.FetchTeamEventByTeamUUIDAndSequenceResponse, error) {

	resolvedOpts := applyAllOptions(opts)
	queries := m.repository.Queries
	if resolvedOpts.SQLTx != nil {
		queries = queries.WithTx(resolvedOpts.SQLTx)
	}
	// A read inside a transaction must observe that transaction's own
	// uncommitted writes, so it can neither be served from the shared cache nor
	// be collapsed into another caller's in-flight query.
	skipShared := resolvedOpts.SkipCache || resolvedOpts.SQLTx != nil
	cacheKey := fmt.Sprintf("FetchTeamEventByTeamUUIDAndSequence:%v", req)
	if !skipShared {
		if cached, found := m.cache.Get(cacheKey); found {
			return cached.(types.FetchTeamEventByTeamUUIDAndSequenceResponse), nil
		}
	}
	fetch := func() (any, error) {
		if req.OrderBy == "" {
			models, err := queries.FetchTeamEventByTeamUUIDAndSequence(
				ctx,
				metichedb.FetchTeamEventByTeamUUIDAndSequenceParams{
					TeamUUID: req.TeamUUID.String(),

					Sequence: req.Sequence,

					Offset: req.Offset,
					Limit:  req.Limit,
				},
			)

			if err != nil {

				return types.FetchTeamEventByTeamUUIDAndSequenceResponse{}, err
			}
			return types.FetchTeamEventByTeamUUIDAndSequenceResponse{
				Results: mapModelsToEntities(models),
			}, nil
		}

		err := errors.New("could not process request")

		return types.FetchTeamEventByTeamUUIDAndSequenceResponse{}, err
	} // end fetch
	var v any
	var fetchErr error
	if skipShared {
		v, fetchErr = fetch()
	} else {
		v, fetchErr, _ = m.sg.Do(cacheKey, fetch)
	}
	if fetchErr != nil {
		return types.FetchTeamEventByTeamUUIDAndSequenceResponse{}, fetchErr
	}
	result := v.(types.FetchTeamEventByTeamUUIDAndSequenceResponse)
	if !skipShared {
		m.cache.Set(cacheKey, result, 0)
	}
	return result, nil

}
