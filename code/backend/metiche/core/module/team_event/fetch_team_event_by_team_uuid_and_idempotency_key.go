// Code generated by nuzur go-code-gen. DO NOT EDIT.

package team_event

import (
	"context"
	"fmt"

	"github.com/mklfarha/metiche/backend/core/module/team_event/types"

	"errors"
	metichedb "github.com/mklfarha/metiche/backend/core/repository/gen"
)

func (m *module) FetchTeamEventByTeamUUIDAndIdempotencyKey(
	ctx context.Context,
	req types.FetchTeamEventByTeamUUIDAndIdempotencyKeyRequest,
	opts ...Option,
) (types.FetchTeamEventByTeamUUIDAndIdempotencyKeyResponse, error) {

	resolvedOpts := applyAllOptions(opts)
	queries := m.repository.Queries
	if resolvedOpts.SQLTx != nil {
		queries = queries.WithTx(resolvedOpts.SQLTx)
	}
	// A read inside a transaction must observe that transaction's own
	// uncommitted writes, so it can neither be served from the shared cache nor
	// be collapsed into another caller's in-flight query.
	skipShared := resolvedOpts.SkipCache || resolvedOpts.SQLTx != nil
	cacheKey := fmt.Sprintf("FetchTeamEventByTeamUUIDAndIdempotencyKey:%v", req)
	if !skipShared {
		if cached, found := m.cache.Get(cacheKey); found {
			return cached.(types.FetchTeamEventByTeamUUIDAndIdempotencyKeyResponse), nil
		}
	}
	fetch := func() (any, error) {
		if req.OrderBy == "" {
			models, err := queries.FetchTeamEventByTeamUUIDAndIdempotencyKey(
				ctx,
				metichedb.FetchTeamEventByTeamUUIDAndIdempotencyKeyParams{
					TeamUUID: req.TeamUUID.String(),

					IdempotencyKey: req.IdempotencyKey,

					Offset: req.Offset,
					Limit:  req.Limit,
				},
			)

			if err != nil {

				return types.FetchTeamEventByTeamUUIDAndIdempotencyKeyResponse{}, err
			}
			return types.FetchTeamEventByTeamUUIDAndIdempotencyKeyResponse{
				Results: mapModelsToEntities(models),
			}, nil
		}

		err := errors.New("could not process request")

		return types.FetchTeamEventByTeamUUIDAndIdempotencyKeyResponse{}, err
	} // end fetch
	var v any
	var fetchErr error
	if skipShared {
		v, fetchErr = fetch()
	} else {
		v, fetchErr, _ = m.sg.Do(cacheKey, fetch)
	}
	if fetchErr != nil {
		return types.FetchTeamEventByTeamUUIDAndIdempotencyKeyResponse{}, fetchErr
	}
	result := v.(types.FetchTeamEventByTeamUUIDAndIdempotencyKeyResponse)
	if !skipShared {
		m.cache.Set(cacheKey, result, 0)
	}
	return result, nil

}
