// Code generated by nuzur go-code-gen. DO NOT EDIT.

package team_event

import (
	"context"
	"fmt"

	"github.com/mklfarha/metiche/backend/core/module/team_event/types"

	"errors"
	metichedb "github.com/mklfarha/metiche/backend/core/repository/gen"

	"github.com/mklfarha/metiche/backend/entity/mapper"
)

func (m *module) FetchTeamEventBySessionUUIDAndSequence(
	ctx context.Context,
	req types.FetchTeamEventBySessionUUIDAndSequenceRequest,
	opts ...Option,
) (types.FetchTeamEventBySessionUUIDAndSequenceResponse, error) {

	resolvedOpts := applyAllOptions(opts)
	queries := m.repository.Queries
	if resolvedOpts.SQLTx != nil {
		queries = queries.WithTx(resolvedOpts.SQLTx)
	}
	// A read inside a transaction must observe that transaction's own
	// uncommitted writes, so it can neither be served from the shared cache nor
	// be collapsed into another caller's in-flight query.
	skipShared := resolvedOpts.SkipCache || resolvedOpts.SQLTx != nil
	cacheKey := fmt.Sprintf("FetchTeamEventBySessionUUIDAndSequence:%v", req)
	if !skipShared {
		if cached, found := m.cache.Get(cacheKey); found {
			return cached.(types.FetchTeamEventBySessionUUIDAndSequenceResponse), nil
		}
	}
	fetch := func() (any, error) {
		if req.OrderBy == "" {
			models, err := queries.FetchTeamEventBySessionUUIDAndSequence(
				ctx,
				metichedb.FetchTeamEventBySessionUUIDAndSequenceParams{
					SessionUUID: mapper.UUIDPtrToNullString(req.SessionUUID),

					Sequence: req.Sequence,

					Offset: req.Offset,
					Limit:  req.Limit,
				},
			)

			if err != nil {

				return types.FetchTeamEventBySessionUUIDAndSequenceResponse{}, err
			}
			return types.FetchTeamEventBySessionUUIDAndSequenceResponse{
				Results: mapModelsToEntities(models),
			}, nil
		}

		err := errors.New("could not process request")

		return types.FetchTeamEventBySessionUUIDAndSequenceResponse{}, err
	} // end fetch
	var v any
	var fetchErr error
	if skipShared {
		v, fetchErr = fetch()
	} else {
		v, fetchErr, _ = m.sg.Do(cacheKey, fetch)
	}
	if fetchErr != nil {
		return types.FetchTeamEventBySessionUUIDAndSequenceResponse{}, fetchErr
	}
	result := v.(types.FetchTeamEventBySessionUUIDAndSequenceResponse)
	if !skipShared {
		m.cache.Set(cacheKey, result, 0)
	}
	return result, nil

}
