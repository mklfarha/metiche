// Code generated by nuzur go-code-gen. DO NOT EDIT.

package server

import (
	"fmt"
	"log"
	"net/http"

	"go.einride.tech/aip/filtering"
	"go.einride.tech/aip/ordering"

	conflict_participantmodule "github.com/mklfarha/metiche/backend/core/module/conflict_participant"
	"github.com/mklfarha/metiche/backend/core/module/conflict_participant/types"
	main_entity "github.com/mklfarha/metiche/backend/entity/conflict_participant"
)

func (h *handlers) ListConflictParticipant(w http.ResponseWriter, r *http.Request) {
	defaultPageSize := int32(10)
	maxPageSize := int32(100)

	lp, err := parseListParams(r, defaultPageSize, maxPageSize)
	if err != nil {
		writeProblem(w, http.StatusBadRequest, err)
		return
	}

	offset, err := parsePageTokenOffset(lp.pageToken)
	if err != nil {
		writeProblem(w, http.StatusBadRequest, fmt.Errorf("invalid page token: %w", err))
		return
	}

	declarations := conflict_participantDeclarations()
	filter, err := filtering.ParseFilter(lp, declarations)
	if err != nil {
		writeProblem(w, http.StatusBadRequest, fmt.Errorf("invalid filter expression: %w", err))
		return
	}

	orderBy, err := ordering.ParseOrderBy(lp)
	if err != nil {
		writeProblem(w, http.StatusBadRequest, fmt.Errorf("invalid order_by expression: %w", err))
		return
	}

	req := types.ListRequest{
		Offset:                offset,
		PageSize:              lp.GetPageSize(),
		Filter:                filter,
		FilteringDeclarations: declarations,
		EnumValues:            conflict_participantEnumValues(),
		OrderBy:               orderBy,
		IncludeFields:         lp.includeFields,
		ExcludeFields:         lp.excludeFields,
	}

	var result types.ListResponse
	if lp.skipCache {
		result, err = h.core.ConflictParticipant().List(r.Context(), req, conflict_participantmodule.WithSkipCache())
	} else {
		result, err = h.core.ConflictParticipant().List(r.Context(), req)
	}
	if err != nil {
		// A filter or order_by the clause builder cannot express is the caller's
		// error, not the server's: statusForError turns it into a 400.
		writeProblem(w, statusForError(err), err)
		return
	}

	resp := ListEnvelope[main_entity.ConflictParticipant]{
		Items: result.ConflictParticipant,
	}
	if result.HasNextPage {
		resp.NextPageToken = encodePageToken(offset + int64(lp.GetPageSize()))
	}

	writeJSON(w, http.StatusOK, resp)
}

func conflict_participantDeclarations() *filtering.Declarations {
	declarations, err := filtering.NewDeclarations(
		filtering.DeclareStandardFunctions(),
		filtering.DeclareIdent("true", filtering.TypeBool),
		filtering.DeclareIdent("false", filtering.TypeBool),

		// conflict_participant

		filtering.DeclareIdent("id", filtering.TypeString),

		filtering.DeclareIdent("conflict_uuid", filtering.TypeString),

		filtering.DeclareIdent("team_uuid", filtering.TypeString),

		filtering.DeclareIdent("session_uuid", filtering.TypeString),

		filtering.DeclareIdent("agent_uuid", filtering.TypeString),

		filtering.DeclareIdent("member_uuid", filtering.TypeString),

		// An enum is filtered by its VALUE over REST — either quoted
		// (status = "active", matched case-insensitively against every spelling
		// in conflict_participantEnumValues) or as the generated value name ident
		// (status = STATUS_ACTIVE, the same spelling gRPC accepts). Both are
		// strings to the type checker; the number they stand for is resolved
		// from the value table, not from this declaration.
		filtering.DeclareIdent("subject_kind", filtering.TypeString),
		filtering.DeclareIdent("SUBJECT_KIND_INVALID", filtering.TypeString),
		filtering.DeclareIdent("SUBJECT_KIND_SESSION", filtering.TypeString),
		filtering.DeclareIdent("SUBJECT_KIND_INTENT", filtering.TypeString),
		filtering.DeclareIdent("SUBJECT_KIND_CLAIM", filtering.TypeString),
		filtering.DeclareIdent("SUBJECT_KIND_CONTRACT", filtering.TypeString),
		filtering.DeclareIdent("SUBJECT_KIND_CONTRACT_ASSERTION", filtering.TypeString),
		filtering.DeclareIdent("SUBJECT_KIND_DECISION", filtering.TypeString),
		filtering.DeclareIdent("SUBJECT_KIND_CONFLICT", filtering.TypeString),
		filtering.DeclareIdent("SUBJECT_KIND_INSTRUCTION", filtering.TypeString),

		filtering.DeclareIdent("subject_uuid", filtering.TypeString),

		// An enum is filtered by its VALUE over REST — either quoted
		// (status = "active", matched case-insensitively against every spelling
		// in conflict_participantEnumValues) or as the generated value name ident
		// (status = STATUS_ACTIVE, the same spelling gRPC accepts). Both are
		// strings to the type checker; the number they stand for is resolved
		// from the value table, not from this declaration.
		filtering.DeclareIdent("role", filtering.TypeString),
		filtering.DeclareIdent("PARTICIPANT_ROLE_INVALID", filtering.TypeString),
		filtering.DeclareIdent("PARTICIPANT_ROLE_INITIATOR", filtering.TypeString),
		filtering.DeclareIdent("PARTICIPANT_ROLE_INCUMBENT", filtering.TypeString),
		filtering.DeclareIdent("PARTICIPANT_ROLE_BYSTANDER", filtering.TypeString),

		filtering.DeclareIdent("notified_at", filtering.TypeTimestamp),

		filtering.DeclareIdent("acked_at", filtering.TypeTimestamp),

		// An enum is filtered by its VALUE over REST — either quoted
		// (status = "active", matched case-insensitively against every spelling
		// in conflict_participantEnumValues) or as the generated value name ident
		// (status = STATUS_ACTIVE, the same spelling gRPC accepts). Both are
		// strings to the type checker; the number they stand for is resolved
		// from the value table, not from this declaration.
		filtering.DeclareIdent("response", filtering.TypeString),
		filtering.DeclareIdent("PARTICIPANT_RESPONSE_INVALID", filtering.TypeString),
		filtering.DeclareIdent("PARTICIPANT_RESPONSE_YIELDED", filtering.TypeString),
		filtering.DeclareIdent("PARTICIPANT_RESPONSE_HELD", filtering.TypeString),
		filtering.DeclareIdent("PARTICIPANT_RESPONSE_UNCLEAR", filtering.TypeString),

		filtering.DeclareIdent("created_at", filtering.TypeTimestamp),

		filtering.DeclareIdent("updated_at", filtering.TypeTimestamp),
	)
	if err != nil {
		log.Printf("error creating declarations for conflict_participant: %v", err)
	}
	return declarations
}

// conflict_participantEnumValues maps each enum filter field to every accepted
// spelling of its values and the number the column stores. REST generates no
// protobuf enum types (an app built with --api rest has no proto package at
// all), so this table — not the filter declarations — is what makes an enum
// filter resolvable here.
func conflict_participantEnumValues() map[string]map[string]int64 {
	return map[string]map[string]int64{

		"subject_kind": {
			"invalid":                         0,
			"SUBJECT_KIND_INVALID":            0,
			"session":                         1,
			"SUBJECT_KIND_SESSION":            1,
			"intent":                          2,
			"SUBJECT_KIND_INTENT":             2,
			"claim":                           3,
			"SUBJECT_KIND_CLAIM":              3,
			"contract":                        4,
			"SUBJECT_KIND_CONTRACT":           4,
			"contract_assertion":              5,
			"SUBJECT_KIND_CONTRACT_ASSERTION": 5,
			"decision":                        6,
			"SUBJECT_KIND_DECISION":           6,
			"conflict":                        7,
			"SUBJECT_KIND_CONFLICT":           7,
			"instruction":                     8,
			"SUBJECT_KIND_INSTRUCTION":        8,
		},

		"role": {
			"invalid":                    0,
			"PARTICIPANT_ROLE_INVALID":   0,
			"initiator":                  1,
			"PARTICIPANT_ROLE_INITIATOR": 1,
			"incumbent":                  2,
			"PARTICIPANT_ROLE_INCUMBENT": 2,
			"bystander":                  3,
			"PARTICIPANT_ROLE_BYSTANDER": 3,
		},

		"response": {
			"invalid":                      0,
			"PARTICIPANT_RESPONSE_INVALID": 0,
			"yielded":                      1,
			"PARTICIPANT_RESPONSE_YIELDED": 1,
			"held":                         2,
			"PARTICIPANT_RESPONSE_HELD":    2,
			"unclear":                      3,
			"PARTICIPANT_RESPONSE_UNCLEAR": 3,
		},
	}
}
