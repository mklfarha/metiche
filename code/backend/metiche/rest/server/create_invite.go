// Code generated by nuzur go-code-gen. DO NOT EDIT.

package server

import (
	"encoding/json"
	"errors"
	"fmt"
	"net/http"

	invitemodule "github.com/mklfarha/metiche/backend/core/module/invite"
	"github.com/mklfarha/metiche/backend/core/module/invite/types"
	main_entity "github.com/mklfarha/metiche/backend/entity/invite"
)

func (h *handlers) CreateInvite(w http.ResponseWriter, r *http.Request) {
	var item main_entity.Invite
	if err := json.NewDecoder(r.Body).Decode(&item); err != nil {
		writeProblem(w, http.StatusBadRequest, err)
		return
	}

	res, err := h.core.Invite().Insert(r.Context(), types.UpsertRequest{
		Invite: item,
	})
	if err != nil {
		writeProblem(w, http.StatusInternalServerError, err)
		return
	}

	fetchRes, err := h.core.Invite().FetchInviteByID(r.Context(), types.FetchInviteByIDRequest(res), invitemodule.WithSkipCache())
	if err != nil {
		writeProblem(w, http.StatusInternalServerError, err)
		return
	}

	if len(fetchRes.Results) == 0 {
		writeProblem(w, http.StatusInternalServerError, errors.New("entity not found after insert"))
		return
	}

	createdEntity := fetchRes.Results[0]

	// Location header
	location := fmt.Sprintf("%s/%v/", r.URL.Path, createdEntity.ID)
	w.Header().Set("Location", location)

	writeJSON(w, http.StatusCreated, createdEntity)
}
