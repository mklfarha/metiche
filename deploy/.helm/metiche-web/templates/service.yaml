apiVersion: v1
kind: Service
metadata:
  name: {{ include "metiche-web.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "metiche-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "metiche-web.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
