apiVersion: v1
kind: Service
metadata:
  name: {{ include "metiche.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "metiche.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "metiche.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
